# IFM Website Fix Checklist
# ─────────────────────────────────────────────
# HOW TO USE:
#   - Change STATUS to one of: PENDING | IN PROGRESS | FIXED
#   - Add new items by copying any block and filling it in
#   - The daily check reads this file and compares against the live site
# ─────────────────────────────────────────────
# Last updated: 2026-08-30

---

## 🔴 CRITICAL BUGS (Broken / Looks wrong to visitors)

### BUG-01 — Footer legal bar duplicated
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** Homepage rebuilt — "Privacy Policy | Terms & Conditions | Share Your Feedback" now appears exactly once in the footer.
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the site is now served as a static export from Vercel and the live homepage is an OLD pre-rebuild snapshot. Rendered footer shows the legal bar TWICE back-to-back ("Privacy Policy | Terms & Conditions | Share Your Feedback" then "Privacy Policy | Terms & Conditions | 💬 Share Your Feedback"). Needs re-fixing in the deployed static build.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the homepage was **redeployed back to the OLD pre-rebuild snapshot** overnight — `content-length: 255,614` (the pre-8-Aug size), `last-modified: Mon, 10 Aug 2026 07:11:36 GMT`. The served footer now contains "Privacy Policy" **×2** and "Share Your Feedback" **×2** — the duplicated legal bar is back. This single rollback also re-breaks BUG-02, BUG-04, SEO-01, SEO-04, UX-01 and UX-02 in one go; re-deploying the 8 Aug build fixes all seven at once.
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20, nothing redeployed:** Twentieth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug**), and the body is byte-for-byte identical to a fetch taken a day earlier. `last-modified` echoing the fetch time with `age: 21` is CDN cache regeneration, not a build. "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not); the rendered DOM also reports "Privacy Policy" twice in the visible footer text. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Twenty days — nearly three weeks — of an avoidable, single-action regression.** /registernow (etag `09f9fd92…`, 27 Aug) and /resources (etag `9a94c23f…`, 10 Aug) are both unchanged since their last builds, so no deploy of any kind has landed since 27 Aug. No check ran on 29 Aug — this run covers both days.
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18, nothing redeployed:** Eighteenth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug**). Fetched cold (`x-vercel-cache: MISS`, `age: 0`), so this is confirmed at origin, not just the CDN edge — `last-modified` echoing the fetch time is cache regeneration, not a build. "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not) — the duplicated legal bar is still live, and the rendered footer confirms both rows back-to-back. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Eighteen days of an avoidable, single-action regression.** Notably, **/registernow WAS redeployed** on 27 Aug (new etag, September & October batches now listed) — so deploys are happening; the homepage simply is not among them. No check ran on 27 Aug — this run covers both days.
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16, nothing redeployed:** Sixteenth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug**). Fetched cold (`x-vercel-cache: MISS`, `age: 0`), so this is confirmed at origin, not just the CDN edge — `last-modified` echoing the fetch time is cache regeneration, not a build. "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not) — the duplicated legal bar is still live. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Sixteen days of an avoidable, single-action regression.**
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15, nothing redeployed:** Fifteenth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug**), `x-vercel-cache: HIT`, `age: 75570`, `last-modified: Mon, 24 Aug 2026 06:33:40 GMT` (CDN cache regeneration, not a build). "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not) — the duplicated legal bar is still live. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Fifteen days — over two weeks — of an avoidable, single-action regression.** No checks ran on 23 or 24 Aug — this run covers all three days.
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12, nothing redeployed:** Twelfth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug**). Fetched with a cold cache (`x-vercel-cache: MISS`, `age: 0`) and the body is byte-for-byte identical to the cached copy, so this is confirmed at origin, not just at the CDN edge — `last-modified` echoing the fetch time is cache regeneration, not a build. "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not) — the duplicated legal bar is still live. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Twelve days of an avoidable, single-action regression.** No check ran on 21 Aug — this run covers both days.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10, nothing redeployed:** Tenth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug**), `x-vercel-cache: HIT`, `age: 80119`, `last-modified: Wed, 19 Aug 2026 05:17:34 GMT` (CDN cache regeneration, not a build). "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not) — the duplicated legal bar is still live. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Ten days — a full sprint — of an avoidable, single-action regression.**
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9, nothing redeployed:** Ninth day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (**unchanged since 11 Aug — this is the reliable signal**), `x-vercel-cache: MISS` with `last-modified` echoing the fetch time, i.e. the CDN regenerating its cache, not a build. "Privacy Policy" ×2, "Share Your Feedback" ×2, "Terms & Conditions" ×2 (one escaped, one not) — the duplicated legal bar is still live. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **Nine days of an avoidable, single-action regression.** No check ran on 18 Aug — this run covers both days.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7, nothing redeployed:** Seventh consecutive day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (unchanged since 11 Aug), `last-modified: Fri, 14 Aug 2026 03:36:16 GMT` (CDN cache regeneration, not a build), `x-vercel-cache: HIT`, `age: 259031`. "Privacy Policy" ×2, "Terms &amp; Conditions" ×2, "Share Your Feedback" ×2 — the duplicated legal bar is still live. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. **One full week of an avoidable, single-action regression.**
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6, nothing redeployed:** Sixth consecutive day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (unchanged since 11 Aug), `x-vercel-cache: HIT`, `age: 173134`. (`last-modified` now reads `Fri, 14 Aug 2026 03:36:16 GMT` — that is the CDN cache regenerating, not a build; the body is byte-for-byte identical to the 11–15 Aug fetches.) "Privacy Policy", "Terms & Conditions" and "Share Your Feedback" each still appear **×2**. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5, nothing redeployed:** Fifth consecutive day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (unchanged since 11 Aug), `x-vercel-cache: HIT`. (The `last-modified` header now tracks the CDN cache regeneration, not a build — the body is byte-for-byte identical to the 11–13 Aug fetches.) "Privacy Policy", "Terms & Conditions" and "Share Your Feedback" each still appear **×2**. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once. No check ran on 14 Aug — this run covers both days.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3, nothing redeployed:** Third consecutive day byte-identical — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"`, `last-modified: Tue, 11 Aug 2026 04:23:56 GMT`, `x-vercel-cache: HIT`. "Privacy Policy", "Terms & Conditions" and "Share Your Feedback" each still appear **×2**. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once.
- **🚨 REGRESSION STILL LIVE (12 Aug 2026) — day 2, nothing redeployed:** The rolled-back build is unchanged — 255,614 bytes, `etag: "a7ff9af0…"`, `last-modified: Tue, 11 Aug 2026 04:23:56 GMT`, `x-vercel-cache: HIT`. "Privacy Policy", "Terms & Conditions" and "Share Your Feedback" each still appear **×2**. All seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) remain regressed; re-deploying the 8 Aug build still fixes all seven at once.
- **✅ REGRESSION RESOLVED (8 Aug 2026):** The 2 Jul rebuild is live again (homepage redeployed, 255,614 → 235,348 bytes, `last-modified: Sat, 08 Aug 2026 03:35:28 GMT`). "Privacy Policy" and "Share Your Feedback" now each appear exactly **once** in the served HTML. Genuinely fixed live.
- **Page:** Homepage (footer)
- **Issue:** "Privacy Policy | Terms & Conditions | Share Your Feedback" row appears TWICE back-to-back.
- **Fix:** Remove one duplicate row in the WordPress footer template.
- **How to verify:** Scroll to bottom of any page — legal bar should appear exactly once.

### BUG-02 — Workshop video thumbnails are blank/black
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** Each workshop `<video>` now uses `preload="metadata"` with a `#t=0.5` media-fragment on the source, so the browser seeks to and paints the 0.5s frame as a still poster instead of a black box.
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the Vercel static homepage now serves 7 `<video preload="metadata" loop muted>` tags with **no `poster=` attribute and no `#t=0.5` fragment** on any `<source>` — the 2 Jul fix is gone. Videos will paint as black boxes again.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the 10 Aug rollback to the old snapshot wiped this again — the homepage serves **7** `<video>` tags with **zero** `poster=` attributes and **zero** `#t=0.5` media fragments. Videos will paint as black boxes again.
- **✅ REGRESSION RESOLVED (8 Aug 2026):** All **7** `<source>` tags carry the `#t=0.5` media fragment again (`IMG_6525.MOV.mp4#t=0.5`, `ifm-workshop-6153-portrait-v2.mp4#t=0.5`, …). The 2 Jul poster-frame fix is back live.
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments (confirmed in source and in the rendered DOM: 7 `<video>` nodes, 0 with a `poster` attribute). Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments (confirmed in both source and rendered DOM). Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments (confirmed in both source and rendered DOM). Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3:** Unchanged — **7** `<video>` tags, **zero** `poster=` attributes, **zero** `#t=0.5` media fragments. Workshop videos still paint as black boxes.
- **Page:** Homepage → "A Glimpse Into Our Workshop" section
- **Issue:** All 4 video containers show as solid black empty boxes. No poster/thumbnail image loads before play.
- **Fix:** Add a `poster="[image-url]"` attribute to each `<video>` tag, OR ensure the video embed platform is loading correctly.
- **How to verify:** Section should show a recognisable still frame from each video before the user clicks play.

### BUG-03 — Curriculum section images not rendering
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** "What You Will Learn" section now serves real photos (WhatsApp-Image JPEGs with object-fit:cover + rounded corners) instead of blank navy rectangles. (Alt text still empty — see SEO-05.)
- **⚠️ Partial regression (28 Jul 2026):** Left FIXED per policy. On the Vercel static homepage the curriculum column still carries a real WhatsApp-Image JPEG (loads 200), so not a full relapse — BUT an asset sweep of all 26 homepage image/video sources found one hard 404: `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png`, used as a `wp-block-cover__image-background`. That cover block will render as a flat blank panel until the file is uploaded to the Vercel deployment.
- **Note (12 Aug 2026) — unchanged, same asset still 404:** `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` (the `wp-block-cover__image-background`) still returns **404** — now 15 days unfixed. Curriculum photos themselves still load. Left FIXED per policy; that one file still needs uploading.
- **Note (11 Aug 2026) — curriculum photos survived the rollback, same asset still 404:** After the 10 Aug rollback to the old snapshot, a full sweep of the 21 `website-assets/images/…` sources on the homepage found exactly **one** hard 404, unchanged since 28 Jul: `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` (the `wp-block-cover__image-background`). All other images return 200, so the curriculum column itself still shows real photos. Left FIXED per policy; that one file still needs uploading.
- **Note (8 Aug 2026) — mostly resolved, one asset still 404:** The curriculum column serves real photos again (`WhatsApp-Image-2026-03-05-at-17.29.32-683x1024.jpeg`, `WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`, both 200). BUT the cover-block background `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` **still returns 404** — unchanged since 28 Jul. That one cover block still renders as a flat blank panel. Left FIXED per policy; the missing file needs uploading to the Vercel deployment.
- **Note (30 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **33 days** unfixed. Every other image returns 200, and the rendered DOM reports **0 broken `<img>` elements** (35 nodes, all with `naturalWidth > 0`), so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (28 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **31 days** unfixed. Every other image returns 200, and the rendered DOM reports 0 broken `<img>` elements, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (26 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **29 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (25 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **34** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **28 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (22 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **25 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (20 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **23 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (19 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **22 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (17 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** Full sweep of the **33** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s — all size variants of one missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **20 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (16 Aug 2026) — unchanged, same asset still 404 (all 4 size variants):** A full sweep of the **38** unique `website-assets/images/…` sources on the homepage found exactly **4** non-200s, and they are all size variants of the same missing source file: `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` plus its `-300x167`, `-768x429` and `-1024x572` derivatives (the `wp-block-cover__image-background` and its srcset). Now **19 days** unfixed. Every other image returns 200, so curriculum photos still load. Left FIXED per policy; uploading that one source image (and letting WP regenerate sizes) clears all four.
- **Note (15 Aug 2026) — unchanged, same asset still 404:** `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` (the `wp-block-cover__image-background`) still returns **404** — now 18 days unfixed. Curriculum photos themselves still load. Left FIXED per policy; that one file still needs uploading.
- **Note (13 Aug 2026) — unchanged, same asset still 404:** `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` (the `wp-block-cover__image-background`) still returns **404** — now 16 days unfixed. Curriculum photos themselves still load. Left FIXED per policy; that one file still needs uploading.
- **Page:** Homepage → "What You Will Learn" / curriculum section
- **Issue:** 4 large navy blue blank rectangles where images should be. Images are missing or broken.
- **Fix:** Re-upload or re-link the 4 image sources in WordPress editor.
- **How to verify:** All 4 boxes should show actual content images (course photos, infographics, etc.).

### BUG-04 — Bullet points rendered as letter "o"
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** Lists now use standard CSS `list-style:disc`; the broken icon-font (which rendered "o") is gone.
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the Vercel static homepage serves the old Word-pasted markup verbatim inside a single `<li>`: "o How money grows over time<br>o How to think about risk…<br>o The basics of investments such as insurance, gold, mutual o funds, and stocks…". Five literal "o" bullets are back (including the stray mid-sentence "mutual o funds"), and there is no `list-style` rule anywhere in the page CSS.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the 10 Aug rollback restored the Word-pasted markup — "mutual o funds" and "o How money grows over time" are both back in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets are live again.
- **✅ REGRESSION RESOLVED (8 Aug 2026):** **Zero** matches for "mutual o funds" or "o How money grows over time" in the served HTML, and a `list-style` rule is present again. The Word-pasted "o" bullets are gone.
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3:** Unchanged — "mutual o funds" (×1) and "o How money grows over time" (×1) are still in the served HTML, and there are **zero** `list-style` rules anywhere in the page CSS. The literal "o" bullets remain live.
- **Page:** Homepage → Curriculum section + "Designed by a Mum" section
- **Issue:** List items show "o" before each point instead of a bullet symbol. Icon font not loading.
- **Fix:** Replace icon-font bullets with standard CSS bullets (`list-style-type: disc`) or Unicode bullet `•`.
- **How to verify:** All bullet lists should show clean bullet symbols, not the letter "o".

### BUG-05 — WhatsApp button and TrustSite badge overlapping
- **Status:** PENDING
- **Note (2 Jul 2026):** Could not verify from source — both the WhatsApp widget and the TrustSite badge are injected at runtime by external scripts (trustedsite.com CDN + WhatsApp plugin), with no positioning in the static HTML. Needs a rendered browser check to confirm overlap. Left PENDING.
- **Note (30 Aug 2026) — still broken, geometry byte-identical to 11–28 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill's z-index is ~1000× higher, so it still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…aSite®" with "CERTIFIED SECURE" below. Twentieth day with not a pixel moved.
- **Note (28 Aug 2026) — still broken, geometry byte-identical to 11–26 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill's z-index is ~1000× higher, so it still paints on top. Eighteenth day with not a pixel moved.
- **Note (26 Aug 2026) — still broken, geometry byte-identical to 11–25 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill's z-index is ~1000× higher, so it still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Sixteenth day with not a pixel moved.
- **Note (25 Aug 2026) — still broken, geometry byte-identical to 11–22 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill's z-index is ~1000× higher, so it still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Fifteenth day with not a pixel moved.
- **Note (22 Aug 2026) — still broken, geometry byte-identical to 11–20 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill's z-index is ~1000× higher, so it still paints on top. (Geometry measured from the live DOM; the screenshot step timed out this run, but every measured value is identical to the previous eleven renders that were visually confirmed.) Twelfth day with not a pixel moved.
- **Note (20 Aug 2026) — still broken, geometry byte-identical to 11–19 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Tenth day with not a pixel moved.
- **Note (19 Aug 2026) — still broken, geometry byte-identical to 11–17 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Ninth day with not a pixel moved.
- **Note (17 Aug 2026) — still broken, geometry byte-identical to 11–16 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Seventh consecutive day with not a pixel moved.
- **Note (16 Aug 2026) — still broken, geometry byte-identical to 11–15 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900 (92×38, `position: fixed`), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890 (240×80, `position: fixed`), `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Sixth consecutive day with not a pixel moved.
- **Note (15 Aug 2026) — still broken, geometry byte-identical to 11–13 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900, `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890, `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…dSite®" with "CERTIFIED SECURE" below. Fifth consecutive day with not a pixel moved.
- **Note (13 Aug 2026) — still broken, geometry byte-identical to 11–12 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900, `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890, `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Third consecutive day with not a pixel moved — the homepage has not been redeployed.
- **Note (12 Aug 2026) — still broken, geometry byte-identical to 11 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 862–900, `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890, `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. Nothing moved — the homepage was not redeployed today.
- **Note (11 Aug 2026) — still broken; badge has slid back down to its 7 Aug position:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y **862–900** (it was y 802–840 on 8–10 Aug — the 10 Aug rollback to the older homepage build moved it back down), `z-index: 1000003`; `#kiwi-big-iframe-wrapper` unchanged at x 1190–1430, y 810–890, `z-index: 999999999`. Boxes intersect over **82×28px** and the pill still paints on top. Bottom-right screenshot confirms the green "Chat with us" pill clipping the badge to "…nSite®" with "CERTIFIED SECURE" below. The badge moving twice in four days is the two homepage builds swapping back and forth — neither build fixes the overlap.
- **Note (10 Aug 2026) — still broken, geometry byte-identical to 8 Aug:** Headless Chrome render at 1440×900. `#trustedsite-tm-image` x 1348–1440, y 802–840, `z-index: 1000003`; `#kiwi-big-iframe-wrapper` x 1190–1430, y 810–890, `z-index: 999999999`. Boxes intersect over **82×30px** and the pill still paints on top. Screenshot of the bottom-right crop confirms the green "Chat with us" pill clipping the badge's "CERTIFIED SECURE" strip. Not a single pixel has moved since 8 Aug — the homepage was not redeployed (same 235,348 bytes, same `last-modified: Sat, 08 Aug 2026 03:35:28 GMT`).
- **Note (8 Aug 2026) — still broken, re-confirmed live with geometry:** Headless Chrome render at 1440×900. The TrustedSite badge has **moved up ~60px** since yesterday (`#trustedsite-tm-image` now x 1348–1440, y 802–840, was y 862–900) but the WhatsApp widget (`#kiwi-big-iframe-wrapper`) is unchanged at x 1190–1430, y 810–890. The boxes still intersect over an **82×30px** region, and the pill's `z-index: 999999999` still beats the badge's `1000003`, so it paints on top. Screenshot of the bottom-right crop confirms the pill clips the badge's "CERTIFIED SECURE" strip to "…ECURE". The badge shifting position changed the shape of the overlap, not the fact of it — this was not a fix.
- **Note (7 Aug 2026) — still broken, re-confirmed live with geometry:** Headless Chrome render at 1440×900. Measured bounding boxes of the fixed bottom-right widgets: TrustedSite badge (`#trustedsite-tm-image`) occupies x 1348–1440, y 862–900 at `z-index: 1000003`; the WhatsApp widget (`#kiwi-big-iframe-wrapper`) occupies x 1190–1430, y 810–890 at `z-index: 999999999`. The boxes intersect over an 82×28px region and the WhatsApp pill's z-index is ~1000× higher, so it paints on top. Screenshot confirms: only "…dSite®" is legible past the pill's right edge, with "CERTIFIED SECURE" below. Unchanged from the 26 Jul – 5 Aug renders.
- **Note (5 Aug 2026) — still broken, re-confirmed live:** Headless Chrome render at 1440×900, bottom-right crop: the green WhatsApp "Chat with us" pill still sits on top of the TrustedSite badge — only "…dSite®" is legible past the pill's lower-right edge, with "CERTIFIED SECURE" below it. Unchanged from the 26 Jul – 1 Aug renders. Both widgets still runtime-injected with no repositioning CSS.
- **Note (1 Aug 2026) — still broken, re-confirmed live:** Headless Chrome render at 1440×900, bottom-right crop: the green WhatsApp "Chat with us" pill still sits on top of the TrustedSite badge — only "…dSite®" is legible to the right of the pill, with "CERTIFIED SECURE" below it. Unchanged from the 26–31 Jul renders. Both widgets still runtime-injected (`cdn.trustedsite.com/js/1.js` + Interakt kiwi-sdk) with no repositioning CSS.
- **Note (31 Jul 2026) — still broken, re-confirmed live:** Headless Chrome render at 1440×900, bottom-right crop: the green WhatsApp "Chat with us" pill still sits on top of the TrustedSite badge — only "…dSite®" is legible behind the pill's lower-right edge, with the "CERTIFIED SECURE" strip below it. Unchanged from the 26–30 Jul renders.
- **Note (30 Jul 2026) — still broken, re-confirmed live:** Headless Chrome render at 1440×900 again shows the green WhatsApp "Chat with us" pill overlapping the TrustedSite badge in the bottom-right — the badge's logo is clipped mid-word to "…dSite®" behind the pill, with only the "CERTIFIED SECURE" strip fully visible below. Unchanged from the 26–29 Jul renders. Both widgets still runtime-injected with no repositioning CSS.
- **Note (28 Jul 2026) — still broken, re-confirmed live:** Headless Chrome render at 1440×900 again shows the green WhatsApp "Chat with us" pill sitting on top of the TrustedSite badge in the bottom-right corner — the badge's logo line is clipped mid-word (only "…dSite®" visible) with the "CERTIFIED SECURE" strip peeking out below. Unchanged from the 26–27 Jul renders. Both widgets are still injected at runtime (`cdn.trustedsite.com/js/1.js` + Interakt WhatsApp SDK) with no repositioning CSS in the page.
- **Note (26 Jul 2026) — OVERLAP NOW CONFIRMED LIVE:** Rendered the homepage in headless Chrome at 1440×900 and screenshotted the bottom-right corner. The green WhatsApp "Chat with us" pill sits on top of the TrustedSite badge: the badge's "TrustedSite®" logo line is clipped mid-word by the pill's right edge, leaving only "…aSite®" and the "CERTIFIED SECURE" strip visible below. This is no longer an unverifiable item — it is a confirmed visual defect. Fix as specified (move TrustedSite to bottom-left, or add bottom offset to the WhatsApp widget).
- **Page:** All pages (bottom-right corner)
- **Issue:** Green WhatsApp "Chat with us" widget and the TrustSite "Certified Secure" badge sit on top of each other.
- **Fix:** Move TrustSite badge to bottom-left, keep WhatsApp bottom-right.
- **How to verify:** Both elements should be fully visible and not overlapping on desktop and mobile.

---

## 🟠 SEO — HIGH PRIORITY (Hurting search ranking every day)

### SEO-01 — No meta description on any page
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** Homepage now carries a real `<meta name="description">`: "Investing for Mummies helps women in Mumbai build financial confidence through practical, jargon-free courses on investing, mutual funds, and personal finance."
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the Vercel static homepage has **zero** `<meta name="description">` tags (regex sweep of the served HTML returns nothing). The 2 Jul description was lost in the migration.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the 10 Aug rollback lost it again — the homepage now has **zero** `<meta name="description">` tags (regex sweep of the served HTML returns nothing).
- **✅ REGRESSION RESOLVED (8 Aug 2026):** Exactly one `<meta name="description">` is back, with the original 2 Jul copy: "Investing for Mummies helps women in Mumbai build financial confidence through practical, jargon-free courses on investing, mutual funds, and personal finance."
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags. /registernow still carries a proper one, so the tooling works; the homepage build is simply the stale one.
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags, and the rendered DOM confirms `document.querySelector('meta[name="description"]')` is null. (For contrast, the freshly redeployed /registernow **does** carry a proper meta description — so the tooling works; the homepage build is simply the stale one.)
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3:** Unchanged — a regex sweep of the served homepage HTML returns **zero** `<meta name="description">` tags.
- **Page:** All pages
- **Issue:** No `<meta name="description">` tags. Google auto-generates one, usually badly.
- **Fix:** Install RankMath or Yoast SEO plugin. Add unique 150-character descriptions to every page.
- **How to verify:** Right-click → View Page Source → search for `meta name="description"`.

### SEO-02 — No sitemap.xml
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** /sitemap.xml now returns a valid XML urlset listing 10 pages (home, about-us, registernow, ifm, ifmintro, iya, ift, resources, privacy-policy, terms-and-conditions).
- **Page:** Site-wide
- **Issue:** `/sitemap.xml` returns 404. Google cannot efficiently discover all pages.
- **Fix:** RankMath/Yoast auto-generates this. Submit to Google Search Console once live.
- **How to verify:** Visit `investingformummies.com/sitemap.xml` — should return an XML list of pages.

### SEO-03 — No Open Graph / social share tags
- **Status:** FIXED
- **Fixed on:** 29 June 2026
- **Note (29 Jun 2026):** og:title, og:description, og:image, og:type, og:url, og:site_name and twitter:card all now present (Yoast). Minor: og:image and og:url use relative paths — switch them to absolute https:// URLs for reliable WhatsApp/LinkedIn previews.
- **⚠️ Caveat still live (28 Jul 2026):** OG tags are still present, but the relative-path problem flagged on 29 Jun is now worse: `og:url` is literally `"/"` and `og:image` is `"website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg"`. Neither resolves for an external scraper, so WhatsApp/LinkedIn previews will show no image. Also `og:title`/`og:description` regressed to the old "Home - Investing For Mummies" + truncated body copy.
- **⚠️ Note (11 Aug 2026) — the half that was fixed on 8 Aug has regressed:** OG tags are still present, but the 10 Aug rollback undid the 8 Aug copy fix: `og:title` is back to **"Home - Investing For Mummies"** and `og:description` is back to the truncated body-copy dump ("Current Batches Your Financial Learning Journey! …"). The original 29 Jun relative-path defect is unchanged: `og:url` is literally `"/"` and `og:image` is the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither resolves for an external scraper — WhatsApp/LinkedIn previews will show no image.
- **Note (8 Aug 2026) — half resolved:** `og:title` and `og:description` are fixed — now "Investing for Mummies — Financial Literacy for Women" and "Practical, jargon-free courses that help women understand investing, take charge of their finances, and join the conversation with confidence." **BUT the original 29 Jun relative-path defect is still live and unchanged:** `og:url` is literally `"/"` and `og:image` is the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither resolves for an external scraper, so WhatsApp/LinkedIn previews will still show no image. Left FIXED per policy — but switching both to absolute `https://` URLs is a genuine outstanding fix.
- **⚠️ Note (30 Aug 2026) — unchanged from 11–28 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies" (the same generic title as SEO-04); `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start You don't…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. Left FIXED per policy (tags exist), but the values are not share-ready.
- **⚠️ Note (28 Aug 2026) — unchanged from 11–26 Aug:** All 5 tags present but degraded (10 `og:*` tags + `twitter:card` in the served HTML). `og:title` = "Home - Investing For Mummies" (the same generic title as SEO-04); `og:url` is the bare relative "/" and `og:image` a relative `website-assets/images/…` path — neither is the absolute URL crawlers and social scrapers need. Left FIXED per policy (tags exist), but the values are not share-ready.
- **⚠️ Note (26 Aug 2026) — unchanged from 11–25 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies" (the same generic title as SEO-04); `og:description` is the auto-generated page-text dump ending in "…[…]"; `og:url` is the bare relative "/" and `og:image` a relative `website-assets/images/…` path — neither is the absolute URL crawlers and social scrapers need. Left FIXED per policy (tags exist), but the values are not share-ready.
- **⚠️ Note (25 Aug 2026) — unchanged from 11–22 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (22 Aug 2026) — unchanged from 11–20 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (20 Aug 2026) — unchanged from 11–19 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (19 Aug 2026) — unchanged from 11–17 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (17 Aug 2026) — unchanged from 11–16 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (16 Aug 2026) — unchanged from 11–15 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (15 Aug 2026) — unchanged from 11–13 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **⚠️ Note (13 Aug 2026) — unchanged from 11–12 Aug:** All 5 tags present but degraded. `og:title` = "Home - Investing For Mummies"; `og:description` = the truncated body-copy dump ("Current Batches Your Financial Learning Journey! Pick A Batch To Start…"); `og:url` = literally `"/"`; `og:image` = the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`. Neither URL resolves for an external scraper — WhatsApp/LinkedIn previews still show no image. `twitter:card` = summary_large_image.
- **Page:** All pages
- **Issue:** When shared on WhatsApp/Instagram/LinkedIn, no image or proper title appears.
- **Fix:** RankMath/Yoast adds these. Set a default OG image (1200×630px branded graphic).
- **How to verify:** Paste URL into https://www.opengraph.xyz and check preview.

### SEO-04 — Homepage title tag is generic
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** Title is now "Investing for Mummies — Financial Literacy Courses for Women in Mumbai" — keyword + location rich, no longer "Home – Investing For Mummies".
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the live `<title>` is back to **"Home - Investing For Mummies"** (and og:title matches). The keyword-rich title was lost in the Vercel migration.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the 10 Aug rollback restored the generic title — the live `<title>` is **"Home - Investing For Mummies"** again, and the duplicate outer/inner title pair is back too (it appears **twice**). The keyword-rich title is gone.
- **✅ REGRESSION RESOLVED (8 Aug 2026):** Live `<title>` is back to "Investing for Mummies — Financial Literacy Courses for Women in Mumbai", and it now appears **once** (the duplicate outer/inner title pair is gone).
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** in the served HTML (the duplicate outer/inner title pair). No keywords, no location.
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** (confirmed as `document.title` in the rendered DOM) and it still appears **twice** in the served HTML. No keywords, no location.
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** in the served HTML. No keywords, no location.
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3:** Unchanged — the live `<title>` is **"Home - Investing For Mummies"** and it still appears **twice** (the duplicate outer/inner title pair). The keyword-rich title is still gone.
- **Page:** Homepage
- **Issue:** Title is "Home – Investing For Mummies". Wastes the most valuable SEO real estate.
- **Fix:** Change to: "Women's Financial Literacy Workshops Mumbai | Investing For Mummies"
- **How to verify:** Check browser tab / View Source for `<title>` tag.

### SEO-05 — Missing alt text on images
- **Status:** PENDING
- **Note (2 Jul 2026):** Still broken — 14 of 16 homepage images have empty `alt=""` (only the logo and an emoji have alt). Curriculum/workshop/testimonial photos all need descriptive alt text.
- **Note (28 Jul 2026):** Still broken — 35 `<img>` tags on the homepage, 33 with empty `alt=""`. Only `alt="Investing for Mummies"` (logo) and `alt="💬"` carry any text.
- **Note (30 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes, 33 empty alt, 0 missing).
- **Note (28 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes, 33 empty alt, 0 missing).
- **Note (26 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes, 33 with no alt text).
- **Note (25 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes, 33 with no alt text).
- **Note (22 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes).
- **Note (20 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes).
- **Note (19 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes).
- **Note (17 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes).
- **Note (16 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Confirmed against the rendered DOM as well (35 `<img>` nodes).
- **Note (15 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`.
- **Note (13 Aug 2026):** Still broken, unchanged — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`.
- **Note (12 Aug 2026):** Still broken, unchanged from 11 Aug — **35** `<img>` tags on the homepage, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`.
- **Note (11 Aug 2026):** Still broken, and numerically worse because of the 10 Aug rollback to the older, image-heavier homepage build: **35** `<img>` tags, **33 with empty `alt=""`**, zero missing the attribute. The only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Same zero descriptive alt text, now across twice as many images.
- **Note (10 Aug 2026):** Still broken, unchanged from 8 Aug — **14 of 16** `<img>` tags have empty `alt=""`, zero have a missing `alt` attribute, and the only two carrying text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`.
- **Note (8 Aug 2026):** Still broken. The redeployed homepage is leaner (16 `<img>` tags, down from 35) but the ratio is unchanged: **14 of 16 have empty `alt=""`**. The only two with text are still `alt="Investing for Mummies"` (logo) and `alt="💬"`. Fewer broken images, same zero descriptive alt text.
- **Page:** All pages
- **Issue:** Multiple images (testimonial photos, hero image, curriculum images) have no alt text.
- **Fix:** Add descriptive alt text to every image in WordPress media library / page editor.
- **How to verify:** Right-click any image → Inspect → look for `alt="..."` attribute with real text.

### SEO-06 — /resources page live but says "Coming Soon"
- **Status:** PENDING
- **Note (2 Jul 2026):** Still broken — /resources body still reads "Coming Soon — Something exciting is on its way. Check back soon!" (This subpage also still serves the OLD footer with the "Products" heading, i.e. it wasn't part of the homepage rebuild.)
- **Note (28 Jul 2026):** Still broken — /resources returns 200 from Vercel and its entire body is 33 lines: nav, "Coming Soon", "Something exciting is on its way. Check back soon!", then the old "Products" footer. No content, no redirect.
- **Note (30 Aug 2026):** Still broken, unchanged — twentieth day byte-identical. /resources returns 200 at 144,873 bytes (`etag: "9a94c23f861f2b9f61fa8f89c9bf1b11"`, unchanged since 10 Aug); stripped of script/style its entire visible body measures **471 characters** of text: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"), and the footer legal bar. Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect. Still listed in sitemap.xml.
- **Note (28 Aug 2026):** Still broken, unchanged — eighteenth day byte-identical. /resources returns 200 at 144,873 bytes (`etag: "9a94c23f861f2b9f61fa8f89c9bf1b11"`, unchanged since 10 Aug); stripped of script/style its entire visible body measures **403 characters** of text: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"), and the footer legal bar. Only `<h1>`s on the page are "Coming Soon" and "Investing For Mummies". Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`), and the `og:description` literally reads "Coming Soon Something exciting is on its way.Check back soon!" — that is what Google and social scrapers see. No content, no redirect. It is also still listed in sitemap.xml.
- **Note (26 Aug 2026):** Still broken, unchanged — sixteenth day byte-identical. /resources returns 200 at 144,873 bytes (`etag: "9a94c23f861f2b9f61fa8f89c9bf1b11"`, unchanged since 10 Aug); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"), and the footer legal bar. Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (25 Aug 2026):** Still broken, unchanged — fifteenth day byte-identical. /resources returns 200 (`etag: "9a94c23f861f2b9f61fa8f89c9bf1b11"`, unchanged since 10 Aug; `x-vercel-cache: MISS` with `last-modified` echoing the fetch time = cache regeneration, not a build); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"), and the footer legal bar. Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (22 Aug 2026):** Still broken, unchanged — twelfth day byte-identical. /resources returns 200 at 144,873 bytes (`etag: "9a94c23f861f2b9f61fa8f89c9bf1b11"`, unchanged); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"), and the footer legal bar. Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (20 Aug 2026):** Still broken, unchanged — tenth day byte-identical. /resources returns 200 at 144,873 bytes (same size and content as 10–19 Aug; `etag: "9a94c23f861f2b9f61fa8f89c9bf1b11"`, `x-vercel-cache: MISS` with `last-modified` echoing the fetch time = CDN cache regeneration, not a build); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (19 Aug 2026):** Still broken, unchanged — ninth day byte-identical. /resources returns 200 at 144,873 bytes (same size and content as 10–17 Aug); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (17 Aug 2026):** Still broken, unchanged — seventh day byte-identical. /resources returns 200 at 144,873 bytes (same size and content as 10–16 Aug); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (16 Aug 2026):** Still broken, unchanged — sixth day byte-identical. /resources returns 200 at 144,873 bytes (same size and content as 10–15 Aug); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (15 Aug 2026):** Still broken, unchanged — fifth day byte-identical. /resources returns 200 at 144,873 bytes (same size and content as 10–13 Aug); stripped of script/style its entire visible body measures **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (13 Aug 2026):** Still broken, unchanged — third day byte-identical. /resources returns 200 at 144,873 bytes (`last-modified: Wed, 12 Aug 2026 03:33:07 GMT`); stripped of script/style its entire visible body is **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect.
- **Note (12 Aug 2026):** Still broken, unchanged. /resources returns 200 at 144,873 bytes (byte-identical to yesterday); stripped of script/style its entire visible body is **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading. Double title still present. No content, no redirect.
- **Note (11 Aug 2026):** Still broken, unchanged. /resources returns 200 at 144,873 bytes; stripped of script/style its entire visible body is **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading. Double title still present. No content, no redirect. (Note: as of today the homepage footer says "Products" too — see UX-01 — so /resources is no longer the odd one out, but for the wrong reason.)
- **Note (10 Aug 2026):** Still broken, unchanged. /resources returns 200 at 144,833 bytes; stripped of script/style its entire visible body is **475 characters**: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (zero "Quick Links"). Double title still present (`Resources — Investing for Mummies` over `Resources - Investing For Mummies`). No content, no redirect. Subpages still have not received the 8 Aug homepage redeploy.
- **Note (8 Aug 2026):** Still broken, and **/resources did not get the homepage's redeploy**. It returns 200 at ~145 KB but its entire visible body is still 471 characters: nav → "Coming Soon" → "Something exciting is on its way. Check back soon!" → the old **"Products"** footer heading (the homepage footer now says "Quick Links"). Still carries the cosmetic double title (`<title>Resources — Investing for Mummies</title>` over an inner `<title>Resources - Investing For Mummies</title>`). No content, no redirect.
- **Page:** /resources
- **Issue:** Page is indexed by Google but has zero content. Damages site quality score.
- **Fix:** Either (a) add real content, or (b) set a 301 redirect to homepage until ready.
- **How to verify:** Visit investingformummies.com/resources — should either have content or redirect.

---

## 🟡 UX & CONTENT (Conversion and trust impact)

### UX-01 — Footer "PRODUCTS" heading is wrong
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (2 Jul 2026):** Homepage footer nav column is now headed "Quick Links". (Caveat: the old /resources subpage still shows the "Products" heading — subpages haven't picked up the new footer yet.)
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the live homepage footer heading is back to **"Products"** — zero occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the 10 Aug rollback restored the old footer — the homepage heading is **"Products"** again, with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources. Back to where it was on 28 Jul.
- **✅ REGRESSION RESOLVED (8 Aug 2026) — homepage only:** The homepage footer heading is "Quick Links" again, with **zero** "Products" headings. ⚠️ The original 2 Jul caveat is back too: **/resources still shows the old "Products" heading** — subpages did not get this redeploy.
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" or "Explore" anywhere in the served HTML. Rendered footer text confirms: "Products / About Us / Registrations / Resources / Games". Same on /resources ("Products" ×1, "Quick Links" ×0).
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18:** Unchanged — the homepage footer nav heading is **"Products"** (×2 in source, rendered as uppercase "PRODUCTS") with **zero** occurrences of "Quick Links" or "Explore". Rendered footer text confirms: "PRODUCTS / About Us / Registrations / Resources / Games".
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16:** Unchanged — the homepage footer nav heading is **"Products"** (×2) with **zero** occurrences of "Quick Links" or "Explore".
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources ("Products" ×2, "Quick Links" ×0).
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12:** Unchanged — the homepage footer nav heading is **"Products"** (×2 in source) with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6:** Unchanged — the homepage footer nav heading is **"Products"** (×2 in source) with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3:** Unchanged — the homepage footer nav heading is **"Products"** with **zero** occurrences of "Quick Links" anywhere in the served HTML. Same on /resources.
- **Page:** All pages (footer)
- **Issue:** The footer nav column is labelled "PRODUCTS" but links to About Us, Registrations, Resources, Games — these are pages, not products.
- **Fix:** Rename label to "QUICK LINKS" or "PAGES".
- **How to verify:** Footer nav column heading should say Quick Links (or similar).

### UX-02 — No contact details in footer
- **Status:** FIXED
- **Fixed on:** 2 July 2026
- **Note (29 Jun 2026):** Physical address now visible in footer (107 Mittal Chambers, Nariman Point, Mumbai 400021). Email and phone still missing.
- **Note (2 Jul 2026):** Now fully resolved — footer includes email (investingformummies@gmail.com) and a clickable phone/WhatsApp number (tel:+91 98670 26583) alongside the address.
- **⚠️ REGRESSION (28 Jul 2026):** Left FIXED per policy, BUT the live footer is back to tagline + Instagram/YouTube icons + "Products" nav + copyright only. No email, no `tel:` link, no address (the Nariman Point address now appears only inside a hidden batch modal, not the footer). Searches for `investingformummies@gmail.com` and `tel:` return nothing in the served HTML.
- **🚨 REGRESSION AGAIN (11 Aug 2026):** Left FIXED per policy, BUT the 10 Aug rollback removed the contact details again — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. The Nariman Point address survives as a single occurrence, but back inside the hidden batch modal rather than the footer. The footer is once again tagline + social icons + "Products" nav + copyright only.
- **✅ REGRESSION RESOLVED (8 Aug 2026):** Footer contact details are back — `mailto:investingformummies@gmail.com` (×2) and `tel:+919867026583`, plus the Nariman Point address (2 occurrences, no longer modal-only).
- **🚨 REGRESSION STILL LIVE (30 Aug 2026) — day 20:** Unchanged — **zero** `mailto:` links, **zero** `tel:` links and **zero** email addresses of any form in the served homepage HTML. The rendered footer text is tagline → Instagram/YouTube → "Products" nav → copyright → legal bar (×2). The Nariman Point address survives only inside the hidden batch modal, not the footer. No contact block of any kind.
- **🚨 REGRESSION STILL LIVE (28 Aug 2026) — day 18:** Unchanged — **zero** `mailto:` links, **zero** `tel:` links, **zero** `wa.me` numbers and **zero** email addresses of any form in the served homepage HTML. The rendered footer is tagline → Instagram/YouTube → "PRODUCTS" nav → copyright → legal bar (×2). No contact block of any kind.
- **🚨 REGRESSION STILL LIVE (26 Aug 2026) — day 16:** Unchanged — **zero** `mailto:` links, **zero** `tel:` links and **zero** email addresses of any form in the served homepage HTML. The only location signal is the word "Mumbai" in body copy (×3), not a footer contact block.
- **🚨 REGRESSION STILL LIVE (25 Aug 2026) — day 15:** Unchanged — **zero** matches for `investingformummies@gmail.com`, **zero** `mailto:` links and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (22 Aug 2026) — day 12:** Unchanged — **zero** matches for `investingformummies@gmail.com`, **zero** `tel:` links and **zero** email addresses of any kind in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (20 Aug 2026) — day 10:** Unchanged — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (19 Aug 2026) — day 9:** Unchanged — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (17 Aug 2026) — day 7:** Unchanged — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (16 Aug 2026) — day 6:** Unchanged — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (15 Aug 2026) — day 5:** Unchanged — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **🚨 REGRESSION STILL LIVE (13 Aug 2026) — day 3:** Unchanged — **zero** matches for `investingformummies@gmail.com` and **zero** `tel:` links in the served HTML. "Nariman Point" survives as a single occurrence, but inside the hidden batch modal rather than the footer. The footer is still tagline + social icons + "Products" nav + copyright only.
- **Page:** All pages (footer)
- **Issue:** No email address, phone number, or physical address visible in footer. Only social icons.
- **Fix:** Add email and/or WhatsApp number to footer. Address (Nariman Point) optional.
- **How to verify:** Footer should show at least one direct contact method.

### UX-03 — "5+ More Batches" claim with no details
- **Status:** PENDING
- **Note (28 Jul 2026):** Still broken, and now worse. The batch section reads "Current Batches / Your Financial Learning Journey! / Pick A Batch To Start" → prose → "5+ More Batches that We Are Offering" → "View All Batches", with **no batch cards at all**. The page ships a `renderBatchCards()` script that fetches `/api/batches`, but (a) the API returns `"live": []`, and (b) the container element `#ifmHomeBatchGrid` it writes into does not exist in the page — the script bails at `if (!grid) return`. Rendered DOM confirms 0 cards. A visitor sees an empty batch section.
- **Note (30 Aug 2026):** Still broken, unchanged — and still contradicting the site's own registration page. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **9+ weeks stale**. Meanwhile /registernow (rebuilt 27 Aug) lists four real dated batches with prices. The homepage still advertises "5+ More Batches" over empty space.
- **Note (28 Aug 2026):** Still broken, unchanged, **and now actively contradicting the site's own registration page.** Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **9 weeks stale**. Meanwhile /registernow was redeployed on 27 Aug and now lists **four real dated batches** (September B1/B2, October B1/B2, with an early-bird price) plus three other programmes. So the homepage advertises "5+ More Batches" over empty space and a July placeholder while the actual schedule is live one click away. Fixing the homepage means either feeding `/api/batches` the same data or adding the missing `#ifmHomeBatchGrid` container.
- **Note (26 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **8+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (25 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **8 weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (22 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (20 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (19 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (17 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (16 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (15 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space. (Note: /registernow *was* redeployed on 13 Aug and now lists more batches — see UX-05 — but none of that reached the homepage.)
- **Note (13 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (12 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **7 weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (11 Aug 2026):** Still broken, and the 10 Aug homepage rollback did not touch it — the defect is identical in both builds. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards; `ifmHomeBatchGrid` appears exactly **once** in source, inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **6+ weeks stale**. Visitors still see "5+ More Batches that We Are Offering" over empty space.
- **Note (10 Aug 2026):** Still broken, unchanged. Rendered DOM: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards. `ifmHomeBatchGrid` appears exactly **once** in the source — inside `renderBatchCards()`'s `getElementById`, with no matching container. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **6+ weeks stale**. A visitor still sees "5+ More Batches that We Are Offering" over empty space.
- **Note (8 Aug 2026):** Still broken — and the homepage redeploy did **not** touch this. Rendered DOM check: `document.getElementById('ifmHomeBatchGrid')` returns **null**, 0 cards. `/api/batches` still returns `"live":[]` with only the `upcoming` entry "July 2026 Batches / Dates & timings to be announced" — now **6 weeks stale** (it is August). The section still reads "5+ More Batches that We Are Offering" over an empty space, with a single "View All Batches" link to /registernow and no inline programme types.
- **Issue:** "5+ More Batches that We Are Offering" appears but clicking "VIEW ALL BATCHES" is the only way to find out. Vague teaser hurts conversion.
- **Fix:** Show a compact summary of the other batch types (Young Adults, Teens, Intro) inline on homepage.
- **How to verify:** Homepage batch section should briefly mention all programme types.

### UX-04 — No FAQ section on registration page
- **Status:** FIXED
- **Fixed on:** 29 June 2026
- **Page:** /registernow
- **Issue:** No answers to common objections: Can I pay in installments? What if I miss a session? Is it online or in-person? What do I need to bring?
- **Fix:** Add a 5-7 question FAQ accordion below the batch listings.
- **How to verify:** Registration page should have a visible FAQ section.

### UX-05 — No email/waitlist capture for full batches
- **Status:** PENDING
- **Note (30 Aug 2026):** Still broken, unchanged — /registernow is byte-identical to the 27 Aug rebuild (`etag: "09f9fd92ac99650232387fa0fa5468eb"`, 29,596 bytes) and still has **zero** occurrences of "waitlist", "wait list" or "notify me", with **7× "Reserve →"** CTAs. The three TBA programmes still show "Next batch — TBA" with a Reserve button and no way to leave an email.
- **Note (28 Aug 2026):** Still broken — **but the page around it changed.** /registernow **was redeployed on 27 Aug** (`etag: "09f9fd92ac99650232387fa0fa5468eb"`, was `"fe7ae5d3…"` from 15–26 Aug) and now lists four real dated batches (Sep B1/B2, Oct B1/B2 with early-bird pricing) plus three other programmes. Despite that rebuild there are still **zero** occurrences of "waitlist", "wait list" or "notify me", and still **7× "Reserve →"** CTAs — the three TBA programmes (Introductory / Young Adults / Teens) each show "Next batch — TBA" with a Reserve button and no way to leave an email. A rebuild happened and the waitlist was not included.
- **Note (26 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to 15–25 Aug (`etag: "fe7ae5d3a62c9855b33601040bd902db"`, unchanged): still **7× "Reserve →"** CTAs. No waitlist or notify-me capture for full or TBA batches.
- **Note (25 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to 15–22 Aug (`etag: "fe7ae5d3a62c9855b33601040bd902db"`, unchanged): still **7× "Reserve →"** CTAs and prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000. The 13 Aug redeploy added batches but no waitlist or notify-me capture for full or TBA batches.
- **Note (22 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to 15–20 Aug (28,872 bytes, `etag: "fe7ae5d3a62c9855b33601040bd902db"`): still **7× "Reserve →"** CTAs. The 13 Aug redeploy added batches but no waitlist or notify-me capture for full or TBA batches.
- **Note (20 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to 15–19 Aug (28,872 bytes, `etag: "fe7ae5d3a62c9855b33601040bd902db"`): still **7× "Reserve →"** CTAs and prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000. The 13 Aug redeploy added batches but no waitlist or notify-me capture for full or TBA batches.
- **Note (19 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to 15–17 Aug (28,872 bytes, `etag: "fe7ae5d3a62c9855b33601040bd902db"`): still **7× "Reserve →"** CTAs and prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000. The 13 Aug redeploy added batches but no waitlist or notify-me capture for full or TBA batches.
- **Note (17 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to 15–16 Aug (28,872 bytes, `last-modified: Thu, 13 Aug 2026 12:02:28 GMT`): still **7× "Reserve →"** CTAs and prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000. The 13 Aug redeploy added batches but no waitlist or notify-me capture for full or TBA batches.
- **Note (16 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. The page is byte-identical to yesterday (28,872 bytes, `etag: "fe7ae5d3…"`, `last-modified: Thu, 13 Aug 2026 12:02:28 GMT`): still **7× "Reserve →"** CTAs and prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000. The 13 Aug redeploy added batches but no waitlist or notify-me capture for full or TBA batches.
- **Note (15 Aug 2026):** Still broken — **zero** occurrences of "waitlist" or "notify me" on /registernow. ⚠️ **The page itself did change** (first movement since 8 Aug): it is now 28,872 bytes, `last-modified: Thu, 13 Aug 2026 12:02:28 GMT`, with **7× "Reserve →"** CTAs (was 5×) and a new **₹12,000** price point alongside ₹21,000→₹19,000, ₹9,500 and ₹8,000. So batches were added — but still no waitlist or notify-me capture for full or TBA batches.
- **Note (13 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow (28,093 bytes, byte-identical to yesterday). CTAs are still **5× plain "Reserve →"**. No capture form for full or TBA batches.
- **Note (12 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow (28,093 bytes). CTAs are still **5× plain "Reserve →"**. No capture form for full or TBA batches.
- **Note (11 Aug 2026):** Still broken — **zero** occurrences of "waitlist" or "notify me" on /registernow. CTAs are now **5× plain "Reserve →"** (the "Reserve B1/B2" labels seen on 8–10 Aug are gone again — /registernow flips between these two variants). No capture form for full or TBA batches.
- **Note (10 Aug 2026):** Still broken, unchanged — **zero** occurrences of "waitlist" or "notify me" on /registernow. CTAs remain "Reserve B1 →", "Reserve B2 →" and 3× plain "Reserve →". No capture form for full or TBA batches.
- **Note (8 Aug 2026):** Still broken — **zero** occurrences of "waitlist" or "notify me" on /registernow. CTAs are "Reserve B1 →", "Reserve B2 →" and 3× plain "Reserve →" (the B1/B2 labels are back after 1–7 Aug showed 5× plain "Reserve →"). /registernow was not part of the homepage redeploy.
- **Note (28 Jul 2026):** Still broken — zero occurrences of "waitlist" on /registernow. Flagship shows "Reserve B1 →" / "Reserve B2 →"; Introductory shows "Next batch — TBA" with no capture form. (Partial credit elsewhere: the homepage's dead batch-modal code contains a "Future Batches - Notify Me" / "Open — leave us your details" state, but it is unreachable — see UX-03.)
- **Issue:** If a batch is full, the lead is lost permanently. No waitlist mechanism.
- **Fix:** Add a simple "Join Waitlist" form (name + email + preferred batch) that triggers when batch shows as full.
- **How to verify:** Full batch should show a waitlist option, not just a dead state.

### UX-06 — Testimonial section hidden behind carousel
- **Status:** PENDING
- **Note (2 Jul 2026):** Still broken — "Voices from Our Community" is a JS click-only carousel showing 2 testimonials per page with dot navigation, not an all-visible grid.
- **Note (28 Jul 2026):** Still broken — 15 `carousel` references in the served homepage (`testimonial-carousel`, `testimonial-track`, dot/arrow nav). Not a grid.
- **Note (30 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions** and 4 elements carrying a `carousel` class. 11 "carousel" references and 23 "testimonial" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (28 Aug 2026):** Still broken, unchanged. The homepage is byte-identical to 11 Aug, so the carousel is definitionally untouched; today's rendered DOM confirms **4 elements carrying a `carousel` class** and 23 `testimonial` references in source, with zero grid/`ifm-voices-*` markup. Still click-only paging, not a grid.
- **Note (26 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 15 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (25 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 15 "carousel" references in source (9 × `testimonial-track`), zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (22 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 11 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (20 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 15 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (19 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 11 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (17 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 15 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (16 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 11 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (15 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 11 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (13 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 15 "carousel" references in source, zero `ifm-voices-*`. Still click-only paging, not a grid.
- **Note (12 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-testimonial-track` is a **4-slide** flex track (358px each, 1,512px of content) inside a **748px** viewport — only **2 cards visible at a time**, with **4 dot-nav positions**. 11 "carousel" references in source (`#ifm-testimonial-carousel`, `.ifm-testimonial-track`, `.ifm-carousel-dots`, `.ifm-carousel-nav`). Still click-only paging, not a grid.
- **Note (11 Aug 2026):** Still broken — and the markup has **reverted to the pre-8-Aug version** with the 10 Aug rollback. The `ifm-voices-*` classes are gone; it is `#ifm-testimonial-carousel` + `.ifm-testimonial-track` again, with `.ifm-carousel-dots` / `.ifm-carousel-nav` and 11 "carousel" references in source. Rendered DOM: a flex track of **4 slides at 358px each (1,432px of content)** with **4 dot-nav positions** — still click-only paging, not an all-visible grid. Third distinct testimonial implementation observed in two weeks; none of them is a grid.
- **Note (10 Aug 2026):** Still broken, unchanged. Rendered DOM: `.ifm-voices-track` holds **10 testimonial cards, only 2 within the viewport** at a time. Source has 38 `ifm-voices-*` references and just 1 stray "carousel" string — a grep for "carousel" will mislead you here; measure visible cards instead.
- **Note (8 Aug 2026):** Still broken. The markup was **rewritten and renamed** in the redeploy — the old `testimonial-carousel`/`testimonial-track` classes are gone, replaced by `ifm-voices-*` (`ifm-voices-track`, `ifm-voices-viewport`, `ifm-voices-dots`, `ifm-voices-nav`, `#ifm-voices-prev`/`#ifm-voices-next`). Rebuilt, not fixed: the source comment literally reads "Voices from Our Community — click-only carousel", cards are `flex:0 0 calc(50% - 12px)` inside an `overflow:hidden` viewport with a `transform` transition. **Rendered DOM confirms 10 testimonial cards, only 2 visible at a time.** Beware: a plain grep for "carousel" now returns only 1 hit — that is a rename, not a fix.
- **Page:** Homepage → Testimonials
- **Issue:** Good testimonials (real names, Mumbai areas) hidden behind a 2-slide carousel. Most users won't click to see more.
- **Fix:** Show all testimonials in a 2 or 3 column grid so they're all visible on load.
- **How to verify:** All testimonials visible without clicking carousel arrows.

### UX-07 — Seat availability / urgency missing from batch cards
- **Status:** PENDING
- **Note (30 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (byte-identical to the 27 Aug rebuild); the only capacity signal is the single FAQ line "Each batch is capped at 10–12 participants". `/api/batches` still exposes `"capacity":6` (still contradicting that FAQ line), so the data exists server-side but no card renders it. Four dated batches with prices and an early-bird deadline, and not one seat count or urgency cue.
- **Note (28 Aug 2026):** Still broken — and this is the clearest miss of the 27 Aug /registernow rebuild. The page was redeployed with four real dated batches, yet there are still **zero** word-boundary matches for "seat"/"seats" anywhere on it; the only capacity signal is the single FAQ line "Each batch is capped at 10–12 participants". `/api/batches` still exposes `"capacity":6` (which also now contradicts the FAQ's 10–12), so the data exists server-side but no card renders it. Four dated batches with prices and an early-bird deadline, and not one seat count or urgency cue.
- **Note (26 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to 15–25 Aug); the only capacity signal is still the single FAQ "capped at" line. `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (25 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to 15–22 Aug); the only capacity signal is still the single FAQ "capped at" line. `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (22 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to 15–20 Aug). `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (20 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to 15–19 Aug). `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (19 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to 15–17 Aug). `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (17 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to 15–16 Aug). `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (16 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow (page byte-identical to yesterday). `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (15 Aug 2026):** Still broken — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow, even though the page was redeployed on 13 Aug with extra batches (see UX-05). `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (13 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow. `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (12 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow. `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (11 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" anywhere on /registernow. `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (10 Aug 2026):** Still broken, unchanged — **zero** word-boundary matches for "seat"/"seats" on /registernow. Only capacity signal is the FAQ line "Each batch is capped at…". `/api/batches` still exposes `"capacity":6`, so the data exists server-side but no page renders it.
- **Note (8 Aug 2026):** Still broken — **zero** matches for "seat" on /registernow; the only capacity signal remains the single FAQ line "Each batch is capped at 10–12 participants". Note the homepage's `/api/batches` now exposes `"capacity":6`, so the data to render "N seats left" exists server-side — but no page reads it (the homepage grid container is still missing, see UX-03).
- **Note (28 Jul 2026):** Still broken on /registernow — batch cards show days/times/dates + "Reserve →" only; the sole capacity mention is the FAQ line "Each batch is capped at 10–12 participants". Worth knowing: the homepage now ships unreachable code for exactly this feature — a batch modal that renders "N seats available" with a fill bar and colour-coded urgency (≤2 orange, ≤5 amber), plus SOLD OUT / REGISTRATION CLOSED states. It never displays (no `#ifmHomeBatchGrid` element, `/api/batches` returns no live batches — see UX-03), and it also drops `seat_limit`/`filled` when normalising the API response, so `left` would always compute to 10. Salvaging this code onto /registernow is probably the cheapest route to closing UX-07.
- **Issue:** "Max 10 participants" is a USP but not shown per batch. No urgency signals.
- **Fix:** Add "X seats remaining" to each batch card. Even "Limited seats" works.
- **How to verify:** Batch cards should show seat count or availability indicator.

---

## 🟢 NICE TO HAVE (Do after above are done)

### NICE-01 — Schema markup (rich snippets)
- **Status:** IN PROGRESS
- **Note (29 Jun 2026):** JSON-LD now present (Yoast default: Organization, WebSite, WebPage, BreadcrumbList, ImageObject). Still no LocalBusiness, Course or Event schema — the types that drive rich results for this business.
- **Note (30 Aug 2026):** Still in progress, unchanged. Homepage carries a single `ld+json` block with the identical `@type` set: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.** /registernow — with four dated, priced batches and 33 FAQ references — still carries **zero** `ld+json` blocks, so the page best suited to Course/Event/FAQPage schema has none at all.
- **Note (28 Aug 2026):** Still in progress, unchanged. Homepage carries a single `ld+json` block with the identical `@type` set: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.** /registernow — rebuilt 27 Aug with four dated, priced batches and a full FAQ — carries **zero** `ld+json` blocks, so the page best suited to Course/Event/FAQPage schema still has none at all.
- **Note (26 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (25 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (22 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (20 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (19 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (17 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (16 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (15 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (13 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (12 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (11 Aug 2026):** Still in progress, unchanged — the rollback made no difference here. Single `ld+json` block, `@type` set identical: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (10 Aug 2026):** Still in progress, unchanged. Single `ld+json` block, `@type` set identical to 8 Aug: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event.**
- **Note (8 Aug 2026):** Still in progress. Parsed the homepage's single `ld+json` block after the redeploy — `@type` set is unchanged: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite. **Still no LocalBusiness, Course or Event** — the three types that would actually drive rich results.
- **Note (28 Jul 2026):** Still in progress — JSON-LD `@type` set on the homepage is WebPage / ImageObject / BreadcrumbList / ListItem / WebSite / Organization / SearchAction / EntryPoint / ReadAction / PropertyValueSpecification. No LocalBusiness, Course or Event. (Note the graph `@id`s point at `investingformummies.com` while the page's own og:url is now `/` — worth reconciling after the Vercel migration.)
- **Page:** Homepage + /registernow
- **Issue:** No LocalBusiness, Course, or Event schema. Missing out on Google rich results.
- **Fix:** RankMath can auto-add Course and LocalBusiness schema.
- **How to verify:** Test with Google Rich Results Tester.

### NICE-02 — Google Analytics 4 + Search Console
- **Status:** FIXED
- **Fixed on:** 29 June 2026
- **Note (29 Jun 2026):** GA4 live via Google Tag Manager — measurement ID G-PCN0R6L3B9 + gtag.js loading on homepage. (Search Console submission not separately verifiable from the front end.)
- **Page:** Site-wide
- **Issue:** No analytics tracking confirmed. Flying blind on traffic sources and user behaviour.
- **Fix:** Install GA4 via WordPress plugin or Google Tag Manager. Verify in GA4 dashboard.
- **How to verify:** Check GA4 Real Time report while browsing the site.

### NICE-03 — Payment/EMI option mentioned on site
- **Status:** PENDING
- **Note (30 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (the only "emi" hits are inside `semi-bold` and `Gemini_Generated_Image`). The 27 Aug rebuild's pricing detail — ₹21,000 flagship, ₹19,000 early bird, ₹12,000 / ₹9,500 / ₹8,000 — still has no payment-flexibility line beside it.
- **Note (28 Aug 2026):** Still broken — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and the freshly rebuilt /registernow (the only "emi" hits are inside `semi-bold` and `Gemini_Generated_Image`). The 27 Aug rebuild did add pricing detail — ₹21,000 flagship, ₹19,000 early bird till 20 Sep, ₹12,000 / ₹9,500 / ₹8,000 for the other programmes — so there is now a clear place to put a payment-flexibility line, and still nothing there.
- **Note (26 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (the only "emi" hits are inside `semi-bold` and `Gemini_Generated_Image`).
- **Note (25 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (22 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (20 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (19 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (17 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (16 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow (prices ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (15 Aug 2026):** Still broken — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow, including on the newly redeployed /registernow (prices now ₹21,000→₹19,000, ₹12,000, ₹9,500, ₹8,000).
- **Note (13 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow.
- **Note (12 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow.
- **Note (11 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow.
- **Note (10 Aug 2026):** Still broken, unchanged — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow. /registernow prices: ₹21,000 → ₹19,000 flagship, ₹9,500, ₹8,000.
- **Note (8 Aug 2026):** Still broken — word-boundary search for instalment / installment / EMI / "payment plan" returns **zero** matches on both the homepage and /registernow.
- **Note (28 Jul 2026):** Still broken — word-boundary search for instalment / installment / EMI / "payment plan" / "pay in 2" returns zero matches on both the homepage and /registernow. Prices shown: flagship ₹21,000 incl. GST (/registernow) and ₹16,000 incl. GST (homepage) — note these two figures still disagree.
- **Issue:** ₹16,000 is a meaningful spend. No mention of installment or payment plan option.
- **Fix:** Add "Pay in 2 instalments available — contact us on WhatsApp" near pricing.
- **How to verify:** Pricing section should mention payment flexibility.

### NICE-04 — Games link to open in new tab
- **Status:** FIXED
- **Fixed on:** 29 June 2026
- **Note (29 Jun 2026):** Games nav link now carries target="_blank" rel="noopener noreferrer" (data-game-patched="true"). Opens the Vercel portal in a new tab.
- **⚠️ Regression (2 Jul 2026):** Left FIXED per policy (do not revert manual FIXED), BUT the rebuilt homepage now serves `<a href="https://ifm-deploy.vercel.app/">Games</a>` with NO target="_blank" — the new-tab behaviour appears lost in the rebuild. Worth re-checking / re-patching.
- **✅ Regression resolved (3 Jul 2026):** Re-verified — the Games nav link now serves `target="_blank" rel="noopener noreferrer" data-game-patched="true"` again. New-tab behaviour restored.
- **Note (30 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (28 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (26 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (25 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (22 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (20 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (19 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (zero `ifm-deploy.vercel.app` references remain). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (17 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (returns 200). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (16 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (returns 200). ⚠️ Still outstanding: the **third** (footer) Games link — `<a href="https://games.investingformummies.com/" style="color:#1a3a5c !important">` — has **no** `target="_blank"`; it will navigate away from the site.
- **Note (15 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (returns 200). ⚠️ Still outstanding: the **third** (footer) Games link has **no** `target="_blank"` — it will navigate away from the site.
- **Note (13 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/` (returns 200). ⚠️ Still outstanding: the **third** (footer) Games link has **no** `target="_blank"` — it will navigate away from the site.
- **Note (12 Aug 2026) — holds, unchanged:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"` and all three still point at the branded `https://games.investingformummies.com/`. ⚠️ Still outstanding: the **third** (footer) Games link has **no** `target="_blank"` — it will navigate away from the site.
- **Note (11 Aug 2026) — holds; one caveat resolved, one persists:** After the 10 Aug rollback the 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"`, so this stays FIXED. ✅ The 8 Aug link-target caveat is resolved: all three Games links now point at the branded `https://games.investingformummies.com/` again (returns 200), not the raw `ifm-deploy.vercel.app`. ⚠️ Still outstanding: the **third** (footer) Games link has **no** `target="_blank"` — it will navigate away from the site.
- **Note (8 Aug 2026) — holds, with two caveats:** The 2 nav Games links still carry `data-game-patched="true" target="_blank" rel="noopener noreferrer"`, so the item stays FIXED. But the redeploy introduced (a) a **third** Games link, `<a href="https://ifm-deploy.vercel.app/">Games</a>`, with **no** `target="_blank"` — it will navigate away from the site; and (b) all three now point back at the raw `ifm-deploy.vercel.app` instead of the branded `games.investingformummies.com` they used on 30 Jul – 7 Aug. The branded subdomain still returns 200 (NICE-05 unaffected), so this is purely the homepage reverting to the older link target.
- **Page:** All pages (nav + footer)
- **Issue:** Games link goes to external Vercel domain. Should open in a new tab so users don't lose the main site.
- **Fix:** Add `target="_blank" rel="noopener"` to the Games nav link.
- **How to verify:** Clicking Games should open a new tab, not navigate away from main site.

### NICE-05 — Subdomain for games portal
- **Status:** FIXED
- **Fixed on:** 17 July 2026
- **Note (17 Jul 2026):** games.investingformummies.com now resolves (HTTP 200) and serves the full IFM game portal — NAV Pizza, ₹1 Crore Ladder, Market Rollercoaster, Cap Climber, IPO Bonanza, Wheel of Gold/Silver, Debt Detective, REIT or Real?, plus SIP/SWP calculators and quizzes. Previously NXDOMAIN/ENOTFOUND on every prior check.
- **Page:** Site architecture
- **Issue:** Games are at `ifm-deploy.vercel.app` — no brand connection in the URL.
- **Fix:** Configure `games.investingformummies.com` as a custom domain pointing to the Vercel deployment.
- **How to verify:** Visiting games.investingformummies.com should load the game portal.

---
## CHANGE LOG
<!-- Daily check bot will append entries here -->
- 2026-08-30: No status changes. Homepage regression now **day 20** — 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` unchanged since 11 Aug, body byte-for-byte identical to a fetch a day earlier. BUG-01/02/04, SEO-01/04, UX-01/02 all still regressed; one redeploy of the 8 Aug build fixes all seven. **No deploy of any kind has landed since 27 Aug** — /registernow (etag `09f9fd92…`) and /resources (etag `9a94c23f…`) are both unchanged too, so last week's "deploys are happening, just not to the homepage" signal has gone quiet as well. BUG-05 overlap geometry identical for the twentieth day (82×28px intersection, screenshot re-confirmed: the green pill clips the badge to "…aSite®"). BUG-03's 4 `Gemini_Generated_Image_ee4fu7…` 404s now 33 days unfixed (38 image sources swept, 0 broken `<img>` in the rendered DOM). SEO-05 (33/35 empty alt), SEO-06 (/resources still "Coming Soon" at 471 visible chars, still in sitemap.xml), UX-03 (`#ifmHomeBatchGrid` still absent, `/api/batches` still `"live":[]` with a July placeholder now 9+ weeks stale), UX-05 (0 waitlist), UX-06 (still a 4-slide/2-visible carousel), UX-07 (0 "seat" matches), NICE-01 (still no LocalBusiness/Course/Event; /registernow still 0 `ld+json`), NICE-03 (0 EMI/instalment matches) all unchanged. 12 of 18 core items FIXED on paper, 7 of those regressed live. No check ran 29 Aug — this run covers both days.
- 2026-08-28: No status changes. Homepage regression now **day 18** — still byte-identical at 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` unchanged since 11 Aug, confirmed at origin with a cold-cache fetch (`x-vercel-cache: MISS`, `age: 0`). BUG-01/02/04, SEO-01/04, UX-01/02 all still regressed; one redeploy of the 8 Aug build fixes all seven. **New signal: /registernow WAS redeployed on 27 Aug** (new etag, now lists four real dated batches — Sep B1/B2, Oct B1/B2 with ₹19,000 early-bird till 20 Sep — plus three other programmes), so deploys are happening and the homepage is simply not among them. That rebuild shipped without waitlist (UX-05), without seat counts (UX-07), without any instalment/EMI line next to its new prices (NICE-03) and without a single `ld+json` block (NICE-01). It also leaves the homepage contradicting the site: "5+ More Batches" over empty space with a July placeholder in `/api/batches`, while the real schedule is one click away (UX-03). BUG-05 overlap geometry identical for the eighteenth day (82×28px intersection). BUG-03's 4 `Gemini_Generated_Image_ee4fu7…` 404s now 31 days unfixed. SEO-05 (33/35 empty alt), SEO-06 (/resources still "Coming Soon", still in sitemap.xml), UX-06 (still a carousel) unchanged. 12 of 18 core items FIXED on paper, 7 of those regressed live. No check ran 27 Aug — this run covers both days.
- 2026-06-29: Checklist created. All 18 items set to PENDING.
- 2026-06-29: First daily check run. UX-04 (FAQ section) confirmed FIXED. UX-02 improved — address added to footer, email/phone still missing. 16 items still pending.
- 2026-06-29: Re-run found additional fixes the first pass missed — SEO-03 (Open Graph + Twitter card tags now present), NICE-02 (GA4 G-PCN0R6L3B9 live), NICE-04 (Games link opens new tab) all FIXED; NICE-01 (basic Yoast schema present, but no LocalBusiness/Course/Event) moved to IN PROGRESS. Core (BUG+SEO+UX) progress: 2 of 18 FIXED (SEO-03, UX-04), UX-02 IN PROGRESS. Confirmed still broken: BUG-01..05, SEO-01/02/04/05/06, UX-01/03/05/06/07.
- 2026-07-02: Homepage was rebuilt — big jump. Newly FIXED: BUG-01, BUG-02, BUG-03, BUG-04, SEO-01, SEO-02, SEO-04, UX-01, UX-02. Core (BUG+SEO+UX) progress: 11 of 18 FIXED. Still pending: BUG-05 (can't verify from source — runtime-injected widgets), SEO-05 (14/16 imgs empty alt), SEO-06 (/resources still "Coming Soon"), UX-03, UX-05, UX-06 (still a carousel), UX-07. NICE-03, NICE-05 pending; NICE-01 still IN PROGRESS. ⚠️ Flags: (1) /resources subpage wasn't part of the rebuild — still old footer + Coming Soon; (2) NICE-04 regression — rebuilt homepage's Games link lost target="_blank".
- 2026-07-04: No status changes — all 10 open items re-verified as still open. SEO-05 (all homepage imgs still empty alt), SEO-06 (/resources still "Coming Soon — Something exciting is on its way"), UX-03 (still just "5+ More Batches that We Are Offering" + View All Batches link, no inline programme types), UX-05 (no waitlist on /registernow), UX-06 (testimonials still a dot-nav carousel), UX-07 (only global "capped at 10–12 participants" line, no per-batch seats), NICE-03 (no EMI/installment mention; pricing shows ₹21,000→₹19,000 only), NICE-05 (games.investingformummies.com still doesn't resolve — ENOTFOUND) all STILL PENDING; BUG-05 still unverifiable from source (runtime-injected widgets); NICE-01 STILL IN PROGRESS. Total unchanged: 12 of 18 FIXED, 5 PENDING core + NICE-03/NICE-05 pending + NICE-01 in progress.
- 2026-07-03: No status changes — all 10 open items re-verified as still open. BUG-05 (runtime widgets, no repositioning CSS — the only `bottom-left` in source is border-radius), SEO-05 (still 14/16 empty alt), SEO-06 (/resources still "Coming Soon"), UX-03, UX-05, UX-06 (still dot carousel), UX-07 (only a global "capped at 10–12 participants" line, no per-batch seats), NICE-03 (no EMI mention), NICE-05 (games subdomain doesn't resolve) all STILL PENDING; NICE-01 STILL IN PROGRESS. ✅ NICE-04 regression from 2 Jul is resolved — Games link now has target="_blank" data-game-patched="true" again. Total: 12 of 18 FIXED (incl. the 3 NICE items), 5 PENDING core + NICE-03/NICE-05 pending + NICE-01 in progress.
- 2026-07-05: No status changes — all 10 open items re-verified as still open. SEO-05 (homepage imgs still empty alt, only logo has descriptive alt), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!"), UX-03 (still "5+ More Batches that We Are Offering" + View All Batches link only, no inline programme types), UX-05 (no waitlist on /registernow), UX-06 (testimonials still a ←→ dot-nav carousel), UX-07 (no per-batch seat availability), NICE-03 (no EMI/installment mention; homepage ₹16,000, /registernow ₹21,000→₹19,000) all STILL PENDING; BUG-05 still unverifiable from source (runtime-injected widgets); NICE-05 (games.investingformummies.com still ENOTFOUND) STILL PENDING; NICE-01 STILL IN PROGRESS. Total unchanged: 12 of 18 FIXED, 5 PENDING core + NICE-03/NICE-05 pending + NICE-01 in progress.
- 2026-07-08: No status changes — all 10 open items re-verified as still open. SEO-05 (homepage images all still empty `alt=""`), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!"), UX-03 (still "5+ More Batches that We are Offering" + single "View All Batches" link, no inline programme types), UX-05 (no waitlist on /registernow), UX-06 (testimonials still a ←→ dot-nav carousel), UX-07 (only global "capped at 10–12 participants", no per-batch seats), NICE-03 (no EMI/installment mention; homepage ₹16,000, /registernow ₹21,000→₹19,000) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected widgets); NICE-05 (games.investingformummies.com still ENOTFOUND) STILL PENDING; NICE-01 STILL IN PROGRESS (JSON-LD not confirmable via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-09: No status changes — all 10 open items re-verified as still open. SEO-05 (homepage still ~15+ images with empty `alt=""`; only logo has descriptive alt), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link, no inline programme types), UX-05 (no waitlist / Join Waitlist form on /registernow), UX-06 (testimonials "Voices from Our Community" still a ←→ arrow-nav carousel), UX-07 (only global "capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment mention; /registernow ₹21,000→₹19,000 flagship) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets); NICE-05 (games.investingformummies.com still ENOTFOUND) STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event schema detected; JSON-LD not confirmable via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-10: No status changes — all 10 open items re-verified as still open. SEO-05 (homepage images still predominantly empty `alt=""` — logo/testimonial/WhatsApp/heart imgs all `alt=""`), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link, no inline programme types), UX-05 (no waitlist / Join Waitlist form on /registernow — "ENROLMENT OPEN FOR AUGUST" with direct reservation only), UX-06 (testimonials "Voices from Our Community" still a ←→ arrow-nav carousel), UX-07 (only global "capped at 10–12 participants" line in FAQ, no per-batch seat counts), NICE-03 (no EMI/installment mention; homepage ₹16,000, /registernow ₹21,000→₹19,000 flagship) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets); NICE-05 (games.investingformummies.com still ENOTFOUND) STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event schema detected; JSON-LD not confirmable via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-11: No status changes — all 10 open items re-verified as still open. SEO-05 (workshop video section has 7 imgs all `alt=""`; homepage images still predominantly empty alt), SEO-06 (/resources still "Coming Soon" / "Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link, no inline programme types), UX-05 (no Join Waitlist form on /registernow — unavailable programmes just show "Next batch — TBA"), UX-06 (testimonials still a ←→ arrow-nav carousel, not a grid), UX-07 (only global "capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment mention; homepage ₹16,000, /registernow ₹21,000→₹19,000 flagship, ₹9,500 intro, ₹1,800 Goa) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets); NICE-05 (games.investingformummies.com still not resolving) STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD detected via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-12: No status changes — all 10 open items re-verified as still open. SEO-05 (homepage images still predominantly empty `alt=""` — logo, workshop photos, testimonial profiles, heart icons, video thumbnails all `alt=""`; no descriptive alt found), SEO-06 (/resources still "Coming Soon" / "Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link → /registernow, no inline programme types), UX-05 (no Join Waitlist form on /registernow — batches show "Reserve" buttons only, no waitlist for unavailable ones), UX-06 (testimonials still a ←→ arrow-nav carousel, not a grid), UX-07 (only global "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment mention; homepage ₹16,000, /registernow ₹21,000→₹19,000 flagship, ₹9,500 intro/young-adults, ₹8,000 teens, ₹1,800 Goa) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets); NICE-05 (games.investingformummies.com still ENOTFOUND) STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD detected via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-14: No status changes — all 10 open items re-verified as still open. SEO-05 (17 imgs on homepage, 15 with empty `alt=""`; only logo has descriptive alt), SEO-06 (/resources still "Coming Soon" / "Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link → /registernow, no inline programme types), UX-05 (no Join Waitlist form on /registernow — only "Reserve" buttons, no option for full cohorts), UX-06 (testimonials "Voices from Our Community" still a ←→ arrow-nav carousel, not a grid), UX-07 (only global "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment mention; /registernow flagship ₹21,000→₹19,000, intro/young-adults ₹9,500, teens ₹8,000, Goa ₹1,800) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets, absent from source); NICE-05 (games.investingformummies.com still ENOTFOUND) STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD detected via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-13: No status changes — all 10 open items re-verified as still open. SEO-05 (all homepage imgs — logo, WhatsApp screenshots, heart icons, testimonial photos, workshop videos — still empty `alt=""`, no descriptive alt), SEO-06 (/resources still "Coming Soon" / "Something exciting is on its way. Check back soon!" + still old "Products" footer heading listing About Us/Batches/Resources/Games), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link → /registernow, no inline programme types), UX-05 (no Join Waitlist form on /registernow — only "Reserve" buttons for available batches + "Next batch — TBA" for unavailable), UX-06 (testimonials "Voices from Our Community" still a ←→ arrow-nav carousel, not a grid), UX-07 (only global "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment mention; homepage ₹16,000, /registernow ₹19,000 flagship / ₹9,500 intro) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets); NICE-05 (games.investingformummies.com still NXDOMAIN — DNS + curl both fail) STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD detected via markdown fetch). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03/NICE-05 also pending.
- 2026-07-17: NICE-05 FIXED 🎉 — games.investingformummies.com now resolves (HTTP 200) and serves the full IFM game portal (NAV Pizza, ₹1 Crore Ladder, Cap Climber, IPO Bonanza, Wheel of Gold/Silver, Debt Detective, REIT or Real?, SIP/SWP calculators, quizzes). This subdomain returned NXDOMAIN/ENOTFOUND on every previous check. All other open items re-verified as STILL open: SEO-05 (homepage images still predominantly empty `alt=""`), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + old "Products" footer), UX-03 (still "5+ More Batches" + single "View All Batches" link, no inline programme types), UX-05 (no Join Waitlist on /registernow — only "Reserve" buttons; flagship ₹21,000→₹19,000, intro ₹12,000, young adults ₹9,500, teens ₹8,000, Goa ₹1,800), UX-06 (testimonials still a ←→ arrow-nav carousel), UX-07 (only global "capped at 10–12 participants", no per-batch seats), NICE-03 (no EMI/installment mention) all STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD). BUG-05 left PENDING — the in-app browser was unresponsive (two 300s navigate timeouts) so the runtime-injected WhatsApp + TrustSite overlap could not be checked live; static HTML still has no repositioning CSS. Total: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-22: No status changes — all 9 open items re-verified as still open. SEO-05 (35 `<img>` tags on the homepage, 33 with empty `alt=""`; only the logo and a 💬 emoji have alt text), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + a single "View All Batches" link, no inline programme types), UX-05 (no waitlist anywhere on /registernow — flagship B1/B2 have "Reserve" buttons, Introductory shows "Next batch — TBA" with no capture; ₹21,000 flagship, ₹12,000 intro, ₹9,500 young adults, ₹8,000 teens, ₹1,800 Goa), UX-06 (testimonials "Voices from Our Community" still uses `ifm-carousel-dots` / `ifm-carousel-nav`, not a grid), UX-07 (only the global FAQ line "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment/payment-plan mention near any price) all STILL PENDING; BUG-05 still unverifiable — static HTML has no repositioning CSS for the runtime-injected WhatsApp + TrustedSite widgets, and the in-app browser timed out again (preview_start 300s), so no rendered check was possible; NICE-01 STILL IN PROGRESS (JSON-LD @graph is WebPage/ImageObject/BreadcrumbList/WebSite/Organization only — no LocalBusiness, Course or Event). Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-20: No status changes — all 9 open items re-verified as still open. SEO-05 (all homepage `<img>` tags still empty `alt=""` — logo, workshop/testimonial photos, workflow graphics, no descriptive alt), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link, no inline programme types), UX-05 (no Join Waitlist form on /registernow — only "Reserve" buttons; Introductory shows "Next batch — TBA" with no waitlist; flagship ₹21,000→₹19,000, intro ₹12,000, young adults ₹9,500, teens ₹8,000, Goa ₹1,800), UX-06 (testimonials "Voices from Our Community" still a ←→ arrow/dot-nav carousel, not a grid), UX-07 (only global "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment/payment-plan mention near pricing) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets, no repositioning CSS in source); NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD confirmable via markdown fetch). Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-25: No status changes — all 9 open items re-verified as still open. SEO-05 (16 imgs on homepage, 14 empty `alt=""`; only logo + heart icon carry alt), SEO-06 (/resources still "Coming Soon" / "Something exciting is on its way. Check back soon!" + still old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + single "View All Batches" link, no inline programme types), UX-05 (no Join Waitlist on /registernow — only "Reserve"/"Reserve B1" buttons; flagship ₹21,000, intro ₹12,000, young adults ₹9,500, teens ₹8,000, Goa ₹1,800), UX-06 (testimonials "Voices from Our Community" still a ←→ arrow-nav carousel, not a grid), UX-07 (only global FAQ line "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment/payment-plan mention; homepage "Course Fee 16k") all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets, no repositioning CSS in source); NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD confirmable via markdown fetch). Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-26: No status changes — all 9 open items re-verified as still open. **BUG-05 overlap CONFIRMED LIVE for the first time** (previous 20+ checks could only say "unverifiable"): headless Chrome render at 1440×900 shows the WhatsApp "Chat with us" pill sitting on top of the TrustedSite badge, clipping its logo mid-word to "…aSite®". Status stays PENDING (still broken), but it is now a verified defect, not an unknown. Also still open: SEO-05 (35 `<img>` tags on the homepage, 33 with empty `alt=""`; only the logo and a 💬 emoji carry alt text), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + still the old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + a single "View All Batches" link, no inline programme types), UX-05 (zero occurrences of "waitlist" anywhere on /registernow — flagship B1/B2 have "Reserve" buttons, Introductory shows "Next batch — TBA" with no capture; ₹21,000 flagship, ₹12,000 intro, ₹9,500 young adults, ₹8,000 teens, ₹1,800 Goa), UX-06 (testimonials still carousel markup — 15 `carousel` references in source, not a grid), UX-07 (only the global FAQ line "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no instalment/EMI/payment-plan mention on homepage or /registernow — word-boundary search returned nothing) all STILL PENDING; NICE-01 STILL IN PROGRESS (JSON-LD @type set is WebPage/ImageObject/BreadcrumbList/ListItem/WebSite/Organization/SearchAction only — no LocalBusiness, Course or Event). Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-27: No status changes — all 9 open items re-verified as still open. **BUG-05 overlap re-confirmed live** (headless Chrome, 1440×900): the green WhatsApp "Chat with us" pill sits on top of the TrustedSite badge in the bottom-right corner, clipping the badge's logo mid-word to "…aSite®" with only the "CERTIFIED SECURE" strip visible below — unchanged from the 26 Jul render. Also still open: SEO-05 (35 `<img>` tags on the homepage, 33 with empty `alt=""`; only `alt="Investing for Mummies"` on the logo and `alt="💬"` carry text), SEO-06 (/resources still "Coming Soon — Something exciting is on its way. Check back soon!" + still the old "Products" footer heading), UX-03 (still "5+ More Batches that We Are Offering" + a single "View All Batches" link, no inline programme types), UX-05 (zero occurrences of "waitlist" anywhere on /registernow — only "Reserve B1 →", "Reserve B2 →" and plain "Reserve →" buttons), UX-06 (testimonials still carousel markup — 14 `carousel` references in homepage source, not a grid), UX-07 (only the global FAQ line "Each batch is capped at 10–12 participants"; word-boundary search for seat counts returned nothing), NICE-03 (word-boundary search for instalment/installment/EMI/payment plan returned zero matches on both the homepage and /registernow) all STILL PENDING; NICE-01 STILL IN PROGRESS (JSON-LD @type set is WebPage/ImageObject/BreadcrumbList/ListItem/WebSite/Organization/SearchAction/EntryPoint/ReadAction/PropertyValueSpecification only — no LocalBusiness, Course or Event). Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-28: **🚨 MAJOR REGRESSION — the site has been re-platformed and is now serving an OLD pre-2-July homepage.** `www.investingformummies.com` no longer serves WordPress: response headers are `server: Vercel`, `content-disposition: inline; filename="home.html"`, all assets under relative `website-assets/…` paths. The deployed snapshot predates the 2 Jul rebuild, so **8 items previously marked FIXED are live-broken again**: BUG-01 (legal bar renders TWICE in the footer), BUG-02 (7 `<video>` tags, no `poster=`, no `#t=0.5` — the 2 Jul fix is gone), BUG-04 (literal "o" bullets back verbatim, incl. the stray "mutual o funds"), SEO-01 (zero `<meta name="description">`), SEO-04 (title back to "Home - Investing For Mummies"), UX-01 (footer heading back to "Products", zero "Quick Links"), UX-02 (no email/phone/address in footer), and SEO-03 is degraded (`og:url` is literally `/`, `og:image` is a relative path — external scrapers resolve neither). All 8 left FIXED per the do-not-revert-manual-statuses policy, but each now carries a ⚠️ REGRESSION note. Also new: (a) the homepage batch section renders **zero batch cards** — `/api/batches` returns `"live": []` AND the `#ifmHomeBatchGrid` container the script writes into doesn't exist in the page, so `renderBatchCards()` bails; (b) one hard 404 among the 26 homepage assets — `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png`, used as a cover background. Meanwhile no open item was fixed: BUG-05 (WhatsApp pill still overlapping and clipping the TrustedSite badge — re-confirmed by headless render at 1440×900), SEO-05 (35 imgs, 33 empty `alt=""`), SEO-06 (/resources still a 33-line "Coming Soon" page with the old "Products" footer), UX-03 (still "5+ More Batches" + one link, now with an empty card grid above it), UX-05 (zero "waitlist" on /registernow), UX-06 (15 carousel refs, still not a grid), UX-07 (no per-batch seats on /registernow — though the homepage ships unreachable "N seats available" modal code for exactly this), NICE-03 (no instalment/EMI mention) all STILL PENDING; NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event). Total on paper unchanged: 13 of 18 FIXED, 5 remaining — but in practice 8 of those 13 are not live right now.
- 2026-07-24: No status changes — all 9 open items re-verified as still open. SEO-05 (homepage still ~16 imgs with only the logo carrying descriptive alt; the other 15 are empty `alt=""`), SEO-06 (/resources still "Coming Soon" / "Something exciting is on its way. Check back soon!" + still the old "Products" footer heading listing About Us/Batches/Resources/Games), UX-03 (still "5+ More Batches that We Are Offering" + a single "View All Batches" link → /registernow, no inline programme types), UX-05 (no Join Waitlist form on /registernow — only "Reserve" buttons; flagship ₹21,000, intro ₹12,000, young adults ₹9,500, teens ₹8,000, Goa ₹1,800), UX-06 (testimonials "Voices from Our Community" still a ←→ dot/arrow-nav carousel, not a grid), UX-07 (only the global "Each batch is capped at 10–12 participants", no per-batch seat counts), NICE-03 (no EMI/installment/payment-plan mention anywhere near pricing) all STILL PENDING; BUG-05 still unverifiable from static HTML (runtime-injected WhatsApp + TrustSite widgets, no repositioning CSS in source); NICE-01 STILL IN PROGRESS (no LocalBusiness/Course/Event JSON-LD confirmable via markdown fetch). Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-07-29: No status changes — all 9 open items re-verified as still open, and the 28 Jul re-platforming regression is **still live** (server: Vercel, `content-disposition: inline; filename="home.html"`, `last-modified: Wed, 29 Jul 2026 03:20:04 GMT` — the file was touched today but the content is unchanged). Regressed-but-marked-FIXED items all still broken on the live site: BUG-01 (legal bar renders twice — 2× "Privacy Policy", 2× "Share Your Feedback"), BUG-02 (7 `<video preload="metadata" loop muted>` tags, **0** with `poster=`, **0** `#t=0.5` fragments), BUG-03 (the cover-block asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still returns **404**), BUG-04 (literal "o How money grows over time" bullets still in source, "mutual o funds" still present, **0** `list-style` rules), SEO-01 (**zero** `<meta name="description">`), SEO-04 (title still "Home - Investing For Mummies"), UX-01 (footer heading still "Products", **0** occurrences of "Quick Links"), UX-02 (no email, no `tel:` link in footer — "Nariman Point" appears only once, inside the hidden batch modal), SEO-03 degraded (`og:url` is literally `/`, `og:image` is the relative path `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`; og:title/og:description still the old auto-generated text). All left FIXED per the do-not-revert policy. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live** — headless Chrome 1440×900 screenshot shows the green WhatsApp "Chat with us" pill sitting on top of the TrustedSite badge, clipping its logo mid-word to "…aSite®" with "CERTIFIED SECURE" peeking out below; unchanged from 26/27/28 Jul), SEO-05 (35 `<img>` tags on the homepage, **33** with empty `alt=""` — only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources now serves a much larger file (145 KB vs the old 33-line page) and has a new outer `<title>Resources — Investing for Mummies</title>`, but the visible body is still just "Coming Soon / Something exciting is on its way. Check back soon!" + the old "Products" footer — cosmetic shell change only, no content), UX-03 (`/api/batches` still returns `"live": []`, and `ifmHomeBatchGrid` still appears **only once** in the page — inside `renderBatchCards()`'s `getElementById`, with no matching container element — so the batch section still renders zero cards above "5+ More Batches that We Are Offering"), UX-05 (**zero** occurrences of "waitlist" on /registernow — flagship B1/B2 show "Reserve B1 →"/"Reserve B2 →", Introductory shows "Next batch — TBA" with no capture), UX-06 (testimonials still carousel markup — `testimonial-carousel`, `testimonial-track`, `testimonial-carousel-js`; 11 `carousel` references, not a grid), UX-07 (**zero** matches for "seats" on /registernow; only the global FAQ line "Each batch is capped at 10–12 participants"), NICE-03 (word-boundary search for instalment/installment/EMI/"payment plan"/"pay in N" returns **nothing** on either the homepage or /registernow; flagship ₹21,000 incl. GST, intro ₹12,000, young adults ₹9,500, teens ₹8,000). NICE-01 STILL IN PROGRESS (JSON-LD `@type` set: WebPage, ImageObject ×2, BreadcrumbList, ListItem, WebSite, Organization, SearchAction, EntryPoint, ReadAction, PropertyValueSpecification — still no LocalBusiness, Course or Event). Total on paper unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — but 8 of those 13 remain not-live.
- 2026-07-30: No status changes — all 9 open items re-verified as still open, and the 28 Jul re-platforming regression is **still live for a third day** (`server: Vercel`, `content-disposition: inline; filename="home.html"`, `last-modified: Thu, 30 Jul 2026 04:53:38 GMT` — the file was touched again today, content unchanged). Regressed-but-marked-FIXED items all still broken live: BUG-01 (2× "Privacy Policy", 2× "Share Your Feedback" in the footer), BUG-02 (7 `<video>` tags, **0** `poster=`, **0** `#t=0` fragments), BUG-03 (cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still **404**), BUG-04 ("o How money grows over time" and "mutual o funds" still in source, **0** `list-style` rules), SEO-01 (**zero** `<meta name="description">`), SEO-04 (title still "Home - Investing For Mummies", ×2), UX-01 (footer heading still "Products", **0** "Quick Links"), UX-02 (**0** matches for `investingformummies@gmail.com`, **0** `tel:` links; "Nariman Point" appears once, only inside the hidden batch modal), SEO-03 degraded (`og:url` literally `/`, `og:image` the relative path `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`; og:title/og:description still the old auto-generated text). All left FIXED per the do-not-revert policy. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live** — headless Chrome 1440×900 screenshot shows the green WhatsApp "Chat with us" pill sitting on top of the TrustedSite badge, clipping its logo mid-word to "…dSite®" with "CERTIFIED SECURE" peeking out below; unchanged from 26–29 Jul), SEO-05 (35 `<img>` tags on the homepage, **33** with empty `alt=""` — only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources still 200 and still 145 KB with the cosmetic outer `<title>Resources — Investing for Mummies</title>`, but the visible body is still "Coming Soon" + "Something exciting is on its way. Check back soon!" + the old "Products" footer — no content, no redirect), UX-03 (`/api/batches` still returns `"live": []` with only an `upcoming` "July 2026 Batches / Dates & timings to be announced" entry, and `ifmHomeBatchGrid` still appears **once** — inside `renderBatchCards()`'s `getElementById`, with no matching container — so zero batch cards render above "5+ More Batches that We Are Offering"), UX-05 (**zero** occurrences of "waitlist" on /registernow — "Reserve B1 →", "Reserve B2 →" and 3× plain "Reserve →" only), UX-06 (testimonials still carousel markup — 9× `testimonial-track`, 5× `carousel-dots`, 3× `carousel-nav`, 2× `testimonial-carousel`; 11 `carousel` references, not a grid), UX-07 (**zero** matches for "seats" on /registernow; only the global FAQ line "Each batch is capped at **10–12 participants**"; prices ₹21,000 / ₹12,000 / ₹9,500 / ₹8,000), NICE-03 (word-boundary search for instalment/installment/EMI/"payment plan"/"pay in N" returns **nothing** on either the homepage or /registernow — the 11 raw "emi" hits are substrings like "premium"/"semi"). NICE-01 STILL IN PROGRESS (JSON-LD `@type` set: WebPage, ImageObject ×2, BreadcrumbList, ListItem, WebSite, Organization, SearchAction, EntryPoint, ReadAction, PropertyValueSpecification — still no LocalBusiness, Course or Event). Sanity checks that remain good: /sitemap.xml returns valid XML (SEO-02), and the Games nav link still carries `target="_blank" rel="noopener noreferrer" data-game-patched="true"` — now pointing at the branded `https://games.investingformummies.com/` (NICE-04, NICE-05). Total on paper unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — but 8 of those 13 remain not-live.
- 2026-07-31: No status changes — all 9 open items re-verified as still open, and the 28 Jul re-platforming regression is **still live for a fourth day** (`server: Vercel`, `content-disposition: inline; filename="home.html"`, `last-modified: Thu, 30 Jul 2026 21:50:52 GMT`). ⚠️ **The homepage file grew to 255,614 bytes overnight** — something was redeployed, but none of the regressed content changed, so the new weight is not a fix. Regressed-but-marked-FIXED items all still broken live: BUG-01 (2× "Privacy Policy", 2× "Share Your Feedback"), BUG-02 (7 `<video>` tags, **0** `poster=`, **0** `#t=0` fragments), BUG-03 (cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still **404**), BUG-04 ("o How money grows over time" and "mutual o funds" still in source, **0** `list-style` rules), SEO-01 (**zero** `<meta name="description">`), SEO-04 (title still "Home - Investing For Mummies", ×2), UX-01 (footer heading still "Products" ×2, **0** "Quick Links"), UX-02 (**0** matches for `investingformummies@gmail.com`, **0** `tel:` links; "Nariman Point" appears once, only inside the hidden batch modal), SEO-03 degraded (`og:url` literally `/`, `og:image` the relative path `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`; og:title still "Home - Investing For Mummies", og:description still truncated body copy). All left FIXED per the do-not-revert policy. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live** — headless Chrome 1440×900 bottom-right crop shows the green WhatsApp "Chat with us" pill on top of the TrustedSite badge, only "…dSite®" legible behind the pill with "CERTIFIED SECURE" below; unchanged from 26–30 Jul), SEO-05 (35 `<img>` tags, **33** with empty `alt=""` — only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources still 200, still ~145 KB, still carries the cosmetic outer `<title>Resources — Investing for Mummies</title>` over an inner `<title>Resources - Investing For Mummies</title>`, but the visible body is still "Coming Soon" + "Something exciting is on its way. Check back soon!" — no content, no redirect), UX-03 (`/api/batches` still returns `"live":[]` with only the `upcoming` "July 2026 Batches / Dates & timings to be announced" entry — now a month stale — and `ifmHomeBatchGrid` still appears **once**, inside `renderBatchCards()`'s `getElementById` with no matching container, so zero cards render above "5+ More Batches that We Are Offering"), UX-05 (**zero** occurrences of "waitlist"/"notify me" on /registernow — "Reserve B1 →", "Reserve B2 →" and 3× plain "Reserve →" only), UX-06 (testimonials still carousel markup — 11 `carousel` references incl. `testimonial-carousel`, `testimonial-track` ×9, `testimonial-carousel-js`; not a grid), UX-07 (**zero** matches for "seat"/"seats" on /registernow; prices ₹21,000 / ₹12,000 / ₹9,500 / ₹8,000), NICE-03 (word-boundary search for instalment/installment/EMI/"payment plan"/"pay in N" returns **nothing** on either the homepage or /registernow). NICE-01 STILL IN PROGRESS (JSON-LD `@type` set: WebPage, ImageObject ×2, BreadcrumbList, ListItem, WebSite, Organization, SearchAction, EntryPoint, ReadAction, PropertyValueSpecification — still no LocalBusiness, Course or Event). Sanity checks that remain good: /sitemap.xml returns 200 XML (SEO-02), games.investingformummies.com returns 200 (NICE-05), and the Games nav link still carries `target="_blank" rel="noopener noreferrer" data-game-patched="true"` (NICE-04). Total on paper unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — but 8 of those 13 remain not-live.
- 2026-08-05: No status changes — all 9 open items re-verified as still open, and the 28 Jul re-platforming regression is **still live, now day 9** (`server: Vercel`, `content-disposition: inline; filename="home.html"`, `last-modified: Wed, 05 Aug 2026 04:58:07 GMT`, still **255,614 bytes** — the file is being touched by redeploys but the byte count is unchanged from 30 Jul, so nothing about the regression has been addressed). Regressed-but-marked-FIXED items all still broken live: BUG-01 (2× "Privacy Policy", 2× "Share Your Feedback" in the footer), BUG-02 (7 `<video>` tags, **0** `poster=`, **0** `#t=0` fragments), BUG-03 (cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still returns **404**), BUG-04 ("o How money grows over time" and "mutual o funds" still in source, **0** `list-style` rules), SEO-01 (**zero** `<meta name="description">`), SEO-04 (title still "Home - Investing For Mummies", ×2), UX-01 (footer heading still "Products" ×2, **0** "Quick Links"), UX-02 (**0** matches for `investingformummies@gmail.com`, **0** `tel:` links; "Nariman Point" appears once, only inside the hidden batch modal), SEO-03 degraded (`og:url` literally `/`, `og:image` the relative path `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`; og:title still "Home - Investing For Mummies", og:description still truncated body copy). All left FIXED per the do-not-revert policy. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live** — headless Chrome 1440×900 bottom-right crop shows the green WhatsApp "Chat with us" pill on top of the TrustedSite badge, only "…dSite®" legible past the pill with "CERTIFIED SECURE" below; unchanged from 26 Jul – 1 Aug), SEO-05 (35 `<img>` tags, **33** with empty `alt=""` — only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources still 200 and ~145 KB, still carries the cosmetic outer `<title>Resources — Investing for Mummies</title>` over an inner `<title>Resources - Investing For Mummies</title>`, but the visible body is still "Coming Soon" + "Something exciting is on its way. Check back soon!" — no content, no redirect), UX-03 (`/api/batches` still returns `"live":[]` with only the `upcoming` "July 2026 Batches / Dates & timings to be announced" entry — now **5 weeks stale** — and `ifmHomeBatchGrid` still appears **once**, inside `renderBatchCards()`'s `getElementById` with no matching container, so zero cards render above "5+ More Batches that We Are Offering"), UX-05 (**zero** occurrences of "waitlist"/"notify me" on /registernow), UX-06 (testimonials still carousel markup — 11 `carousel` references, `testimonial-track` ×9; not a grid), UX-07 (**zero** matches for "seat"/"seats" on /registernow; only the global FAQ line "Each batch is capped at…"), NICE-03 (word-boundary search for instalment/installment/EMI/"payment plan"/"pay in N" returns **nothing** on either the homepage or /registernow). NICE-01 STILL IN PROGRESS (JSON-LD `@type` set: WebPage, ImageObject ×2, BreadcrumbList, ListItem, WebSite, Organization, SearchAction, EntryPoint, ReadAction, PropertyValueSpecification — still no LocalBusiness, Course or Event). One cosmetic change on /registernow: the batch CTAs are now 5× "Reserve →" (7× on 1 Aug) — two batch cards dropped, not a fix. Sanity checks that remain good: /sitemap.xml returns 200 XML with 10 URLs (SEO-02), the Games nav link still carries `target="_blank" rel="noopener noreferrer" data-game-patched="true"` pointing at `https://games.investingformummies.com/` (NICE-04, NICE-05), and /registernow still has its FAQ section (UX-04). Total on paper unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — but 8 of those 13 remain not-live.
- 2026-08-07: No status changes — all 9 open items re-verified as still open, and the 28 Jul re-platforming regression is **still live, now day 11** (`server: Vercel`, `content-disposition: inline; filename="home.html"`, `last-modified: Fri, 07 Aug 2026 06:58:04 GMT`, still **255,614 bytes** — byte-count identical to every check since 30 Jul, so redeploys keep touching the file without changing anything). Regressed-but-marked-FIXED items all still broken live: BUG-01 (2× "Privacy Policy", 2× "Share Your Feedback" in the footer), BUG-02 (7 `<video>` tags, **0** `poster=` attributes), BUG-03 (cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still returns **404**), BUG-04 ("mutual o funds" still in source, **0** `list-style` rules), SEO-01 (**zero** `<meta name="description">`), SEO-04 (title still "Home - Investing For Mummies", ×2), UX-01 (footer heading still "Products" ×2, **0** "Quick Links"), UX-02 (**0** matches for `investingformummies@gmail.com`, **0** `tel:` links), SEO-03 degraded (`og:url` literally `/`, `og:image` the relative path `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`; og:title still "Home - Investing For Mummies", og:description still truncated body copy). All left FIXED per the do-not-revert policy. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live, now with measured geometry** — TrustedSite badge box x 1348–1440 / y 862–900 at `z-index:1000003` vs WhatsApp widget x 1190–1430 / y 810–890 at `z-index:999999999`; the boxes intersect over 82×28px and the pill paints on top, clipping the badge to "…dSite®"), SEO-05 (35 `<img>` tags, **33** with empty `alt=""` — only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources still 200 and ~145 KB with the cosmetic outer `<title>Resources — Investing for Mummies</title>` over an inner `<title>Resources - Investing For Mummies</title>`, but the visible body is still "Coming Soon" — no content, no redirect), UX-03 (`/api/batches` still returns `"live":[]` with only the `upcoming` "July 2026 Batches / Dates & timings to be announced" entry — now **6 weeks stale** — and `ifmHomeBatchGrid` still appears **once**, inside `renderBatchCards()`'s `getElementById` with no matching container, so zero cards render above "5+ More Batches that We Are Offering"), UX-05 (**zero** occurrences of "waitlist"/"notify me" on /registernow — 5× plain "Reserve →" only), UX-06 (testimonials still carousel markup — 10 `carousel` references, not a grid), UX-07 (**zero** matches for "seat" on /registernow; prices ₹21,000 → ₹19,000 flagship, ₹12,000 intro, ₹9,500 young adults, ₹8,000 teens), NICE-03 (word-boundary search for instalment/installment/EMI/"payment plan" returns **nothing** on either the homepage or /registernow). NICE-01 STILL IN PROGRESS (JSON-LD `@type` set: WebPage, ImageObject ×2, BreadcrumbList, ListItem, WebSite, Organization, SearchAction, EntryPoint, ReadAction, PropertyValueSpecification — still no LocalBusiness, Course or Event). Sanity checks that remain good: /sitemap.xml returns 200 XML with 10 URLs (SEO-02), the Games nav link still carries `target="_blank" rel="noopener noreferrer" data-game-patched="true"` pointing at `https://games.investingformummies.com/` (NICE-04, NICE-05), and /registernow still has its FAQ section (UX-04). Total on paper unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — but 8 of those 13 remain not-live.
- 2026-08-08: **🎉 THE 28 JUL RE-PLATFORMING REGRESSION IS RESOLVED — the 2 July rebuild is live again.** The homepage was genuinely redeployed for the first time since 30 Jul: **255,614 → 235,348 bytes** (the byte count had been frozen for 9 days while redeploys merely touched the file), `last-modified: Sat, 08 Aug 2026 03:35:28 GMT`. All 8 regressed-but-marked-FIXED items are now genuinely fixed on the live site: BUG-01 ("Privacy Policy" and "Share Your Feedback" each appear exactly **once** in the footer, was 2×), BUG-02 (all **7** `<source>` tags carry the `#t=0.5` poster fragment again, was 0), BUG-04 (**zero** "mutual o funds" / "o How money grows over time"; a `list-style` rule is present again), SEO-01 (the 2 Jul `<meta name="description">` is back), SEO-04 (title back to "Investing for Mummies — Financial Literacy Courses for Women in Mumbai", and now appears once — the duplicate outer/inner title pair is gone), UX-01 (footer heading "Quick Links", **0** "Products"), UX-02 (`mailto:investingformummies@gmail.com` ×2, `tel:+919867026583`, Nariman Point address ×2 — no longer modal-only), and SEO-03 partially (og:title/og:description restored to the good copy). **Three things the redeploy did NOT fix**, all pre-existing: (a) SEO-03's original 29 Jun defect survives — `og:url` is still literally `/` and `og:image` still the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`, so external scrapers still resolve neither and WhatsApp/LinkedIn previews still show no image; (b) BUG-03's cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` **still returns 404** (curriculum photos themselves load fine); (c) **subpages did not get the redeploy** — /resources and /registernow are unchanged, and /resources still shows the old "Products" footer heading. No genuinely-open item was fixed; all 9 re-verified as still open: BUG-05 (**overlap re-confirmed live with geometry** — the TrustedSite badge moved up ~60px, `#trustedsite-tm-image` now x 1348–1440 / y 802–840 vs y 862–900 yesterday, but `#kiwi-big-iframe-wrapper` is unchanged at x 1190–1430 / y 810–890, so the boxes still intersect over **82×30px** and the pill's `z-index:999999999` still beats the badge's `1000003`; screenshot shows the pill clipping "CERTIFIED SECURE" to "…ECURE" — the badge moving changed the shape of the overlap, not the fact of it), SEO-05 (the leaner page has 16 `<img>` tags, down from 35, but **14 of 16** still have empty `alt=""` — only the logo and `alt="💬"` carry text), SEO-06 (/resources still 200 at ~145 KB with a 471-character body: "Coming Soon" + "Something exciting is on its way. Check back soon!" + the old "Products" footer; no content, no redirect), UX-03 (rendered DOM: `getElementById('ifmHomeBatchGrid')` returns **null**, **0** cards; `/api/batches` still `"live":[]` with only the "July 2026 Batches / Dates & timings to be announced" upcoming entry — now **6 weeks stale**), UX-05 (**zero** "waitlist"/"notify me" on /registernow; CTAs back to "Reserve B1 →"/"Reserve B2 →" + 3× "Reserve →"), UX-06 (⚠️ **rewritten, not fixed** — the testimonial markup was renamed from `testimonial-carousel`/`testimonial-track` to `ifm-voices-*`, so a naive grep for "carousel" now returns just 1 hit, but the source comment says "click-only carousel" and the **rendered DOM shows 10 cards with only 2 visible at a time**), UX-07 (**zero** matches for "seat" on /registernow; note `/api/batches` now exposes `"capacity":6`, so the data exists but no page renders it), NICE-03 (**zero** matches for instalment/installment/EMI/"payment plan" on either page). NICE-01 STILL IN PROGRESS (single `ld+json` block, `@type` set unchanged: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite — still no LocalBusiness, Course or Event). Sanity checks that remain good: /sitemap.xml returns 200 XML with 10 URLs (SEO-02), games.investingformummies.com returns 200 (NICE-05), /registernow still has its FAQ section (UX-04, 33 "faq" refs). One new nit on NICE-04: the redeploy added a **third** Games link with no `target="_blank"` (the 2 nav links are still patched), and all three now point at the raw `ifm-deploy.vercel.app` instead of the branded subdomain they used 30 Jul – 7 Aug. Total: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — **and for the first time since 28 Jul, all 13 "FIXED" items are actually live.**
- 2026-08-10: No status changes — all 9 open items re-verified as still open. **The site has not been touched since the 8 Aug redeploy**: the homepage is byte-identical (235,348 bytes, `etag: "45def929…"`, `last-modified: Sat, 08 Aug 2026 03:35:28 GMT`, `x-vercel-cache: HIT`), so today's check is a clean re-confirmation rather than a new state. The 8 Aug restoration still holds — all 8 previously-regressed FIXED items remain genuinely live: BUG-01 ("Privacy Policy" / "Terms & Conditions" / "Share Your Feedback" each appear exactly **once**), BUG-02 (7 `<video>` tags, all **7** `<source>`s carry `#t=0.5`), BUG-04 (**0** "mutual o funds" / "o How money grows", 1 `list-style` rule), SEO-01 (the 2 Jul `<meta name="description">` present, ×1), SEO-04 (title "Investing for Mummies — Financial Literacy Courses for Women in Mumbai", ×1), UX-01 (footer heading "Quick Links", **0** "Products"), UX-02 (`investingformummies@gmail.com` ×2, `tel:` ×1, "Nariman Point" ×2), SEO-03 partially (og:title/og:description are the good copy). Three pre-existing defects the 8 Aug redeploy did not fix are all still live: (a) SEO-03's 29 Jun relative-path defect — `og:url` is literally `/` and `og:image` the relative `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`, so WhatsApp/LinkedIn previews still resolve no image; (b) BUG-03's cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` **still returns 404** (13 days now); (c) **subpages still have not received the redeploy** — /resources and /registernow are unchanged and /resources still serves the old "Products" footer heading. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live, geometry unchanged to the pixel** — `#trustedsite-tm-image` x 1348–1440 / y 802–840 at `z-index:1000003` vs `#kiwi-big-iframe-wrapper` x 1190–1430 / y 810–890 at `z-index:999999999`; **82×30px** intersection, pill paints on top and clips the badge's "CERTIFIED SECURE" strip — screenshot confirms), SEO-05 (16 `<img>` tags, **14** with empty `alt=""`; only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources 200 at 144,833 bytes but only **475 characters** of visible body — "Coming Soon" + "Something exciting is on its way. Check back soon!" + the old "Products" footer; no content, no redirect), UX-03 (rendered DOM: `getElementById('ifmHomeBatchGrid')` returns **null**, **0** cards; `/api/batches` still `"live":[]` with only the "July 2026 Batches / Dates & timings to be announced" upcoming entry — **6+ weeks stale** in August), UX-05 (**zero** "waitlist"/"notify me" on /registernow; CTAs "Reserve B1 →", "Reserve B2 →" + 3× "Reserve →"), UX-06 (**rendered DOM: 10 testimonial cards, only 2 visible at a time** — 38 `ifm-voices-*` refs and just 1 stray "carousel" string, so grep alone would mislead), UX-07 (**zero** "seat"/"seats" matches on /registernow; `/api/batches` exposes `"capacity":6` but no page renders it), NICE-03 (**zero** instalment/installment/EMI/"payment plan" matches on either page). NICE-01 STILL IN PROGRESS (single `ld+json`, `@type` set unchanged: BreadcrumbList, EntryPoint, ImageObject, ListItem, Organization, PropertyValueSpecification, ReadAction, SearchAction, WebPage, WebSite — still no LocalBusiness, Course or Event). Sanity checks that remain good: /sitemap.xml returns 200 XML with 10 URLs (SEO-02), games.investingformummies.com returns 200 (NICE-05), /registernow still has its FAQ section (UX-04, 33 "faq" refs). NICE-04 caveat from 8 Aug persists: the third Games link `<a href="https://ifm-deploy.vercel.app/">Games</a>` still has **no** `target="_blank"`, and all Games links still point at the raw Vercel domain rather than the branded subdomain. Total unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending.
- 2026-08-01: No status changes — all 9 open items re-verified as still open, and the 28 Jul re-platforming regression is **still live for a fifth day** (`server: Vercel`, `content-disposition: inline; filename="home.html"`, `last-modified: Fri, 31 Jul 2026 18:29:25 GMT`, still 255,614 bytes — byte-identical in size to yesterday, so nothing about the regression was touched). Regressed-but-marked-FIXED items all still broken live: BUG-01 (2× "Privacy Policy", 2× "Share Your Feedback" in the footer), BUG-02 (7 `<video>` tags, **0** `poster=`, **0** `#t=0` fragments), BUG-03 (cover asset `website-assets/images/Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still returns **404**), BUG-04 ("o How money grows over time" and "mutual o funds" still in source, **0** `list-style` rules), SEO-01 (**zero** `<meta name="description">`), SEO-04 (title still "Home - Investing For Mummies", ×2), UX-01 (footer heading still "Products" ×2, **0** "Quick Links"), UX-02 (**0** matches for `investingformummies@gmail.com`, **0** `tel:` links; "Nariman Point" appears once, only inside the hidden batch modal), SEO-03 degraded (`og:url` literally `/`, `og:image` the relative path `website-assets/images/WhatsApp-Image-2026-03-10-at-17.00.43.jpeg`; og:title still "Home - Investing For Mummies"). All left FIXED per the do-not-revert policy. Genuinely open items, all re-confirmed: BUG-05 (**overlap re-confirmed live** — headless Chrome 1440×900 bottom-right crop shows the green WhatsApp "Chat with us" pill on top of the TrustedSite badge, only "…dSite®" legible beside it with "CERTIFIED SECURE" below; unchanged from 26–31 Jul), SEO-05 (35 `<img>` tags, **33** with empty `alt=""` — only `alt="Investing for Mummies"` and `alt="💬"` carry text), SEO-06 (/resources still 200 and ~145 KB, still carries the cosmetic outer `<title>Resources — Investing for Mummies</title>` over an inner `<title>Resources - Investing For Mummies</title>`, but the visible body is still "Coming Soon" + "Something exciting is on its way. Check back soon!" — no content, no redirect), UX-03 (`/api/batches` still returns `"live":[]` with only the `upcoming` "July 2026 Batches / Dates & timings to be announced" entry — now a full month stale — and `ifmHomeBatchGrid` still appears **once**, inside `renderBatchCards()`'s `getElementById` with no matching container, so zero cards render above "5+ More Batches that We Are Offering"), UX-05 (**zero** occurrences of "waitlist"/"notify me" on /registernow), UX-06 (testimonials still carousel markup — 11 `carousel` references, `testimonial-track` ×9; not a grid), UX-07 (**zero** matches for "seat"/"seats" on /registernow; only the global FAQ line "Each batch is capped at…"), NICE-03 (word-boundary search for instalment/installment/EMI/"payment plan"/"pay in N" returns **nothing** on either the homepage or /registernow). NICE-01 STILL IN PROGRESS (JSON-LD `@type` set: WebPage, ImageObject ×2, BreadcrumbList, ListItem, WebSite, Organization, SearchAction, EntryPoint, ReadAction, PropertyValueSpecification — still no LocalBusiness, Course or Event). One cosmetic change on /registernow: the batch CTAs are now 7× plain "Reserve →" (the "Reserve B1 →"/"Reserve B2 →" labels are gone) — a label change, not a fix. Sanity checks that remain good: /sitemap.xml returns 200 XML (SEO-02), the Games nav link still carries `target="_blank" rel="noopener noreferrer" data-game-patched="true"` pointing at `https://games.investingformummies.com/` (NICE-04, NICE-05), and /registernow still has its FAQ section (UX-04). Total on paper unchanged: 13 of 18 FIXED, 5 remaining (4 pending core + NICE-01 in progress); NICE-03 also pending — but 8 of those 13 remain not-live.
- 2026-08-12: No status changes — all 9 open items re-verified as still open, and **the 10 Aug rollback is still live (day 2)**. The homepage is byte-identical to yesterday: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"`, `last-modified: Tue, 11 Aug 2026 04:23:56 GMT`, `x-vercel-cache: HIT` — nothing was redeployed. The seven items the rollback re-broke are all still regressed: BUG-01 (Privacy Policy / Terms / Share Your Feedback each ×2), BUG-02 (7 `<video>` tags, 0 posters, 0 `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time" back, 0 `list-style` rules), SEO-01 (0 meta descriptions), SEO-04 (`<title>Home - Investing For Mummies</title>` ×2), UX-01 ("Products" ×2, 0 "Quick Links"), UX-02 (0 gmail, 0 `tel:`, address modal-only). **Re-deploying the 8 Aug build fixes all seven at once — this is the single highest-value action available.** Open items re-confirmed with fresh evidence: BUG-05 (headless render — TrustedSite badge x 1348–1440/y 862–900 vs WhatsApp pill x 1190–1430/y 810–890, **82×28px overlap**, pill's z-index 999999999 beats 1000003; screenshot confirms), SEO-05 (33 of 35 imgs empty `alt=""`), SEO-06 (/resources 475-char body, still "Coming Soon" + old "Products" footer), UX-03 (`#ifmHomeBatchGrid` null, 0 cards, `/api/batches` `"live":[]` with a **7-week-stale** July upcoming entry), UX-05 (0 "waitlist"/"notify me", 5× plain "Reserve →"), UX-06 (rendered DOM: 4-slide 1,512px track in a 748px viewport = **2 cards visible**, 4 dots), UX-07 (0 "seat"/"seats" on /registernow despite `"capacity":6` in the API), NICE-01 (no LocalBusiness/Course/Event), NICE-03 (0 EMI/instalment matches). Also unchanged: the `Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` cover asset still 404s (15 days), and the third/footer Games link still lacks `target="_blank"`.
- 2026-08-11: 🚨 **MAJOR REGRESSION — the homepage was rolled back to the old pre-rebuild snapshot** (`content-length` 235,348 → 255,614, `last-modified: Mon, 10 Aug 2026 07:11:36 GMT`). This single rollback undid the 8 Aug redeploy and re-broke **seven** items at once: BUG-01 (legal bar duplicated again), BUG-02 (7 videos, no posters/`#t=0.5`), BUG-04 ("o" bullets back), SEO-01 (no meta description), SEO-03 (og:title/description regressed), SEO-04 (title back to "Home - Investing For Mummies"), UX-01 ("Products" footer heading), UX-02 (no email/phone in footer). All left FIXED per the do-not-revert policy, each with a regression note — **re-deploying the 8 Aug build fixes all seven at once.** No status changes; no genuinely new fixes. All 9 open items re-verified as still open: BUG-05 (82×28px overlap, badge slid back to y862–900), SEO-05 (now 33 of 35 imgs with empty alt), SEO-06 (/resources still 475-char "Coming Soon"), UX-03, UX-05 (CTAs back to 5× plain "Reserve →"), UX-06 (testimonial markup reverted to `#ifm-testimonial-carousel`, 4 slides / 4 dots), UX-07, NICE-03 PENDING; NICE-01 IN PROGRESS. ✅ One small win: NICE-04's Games links point at the branded `games.investingformummies.com` again (footer link still lacks `target="_blank"`). Totals unchanged: 12 of 18 FIXED.
- 2026-08-13: No status changes — all 9 open items re-verified as still open, and **the 10 Aug rollback is still live (day 3)**. The homepage is byte-identical for the third consecutive day: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"`, `last-modified: Tue, 11 Aug 2026 04:23:56 GMT`, `x-vercel-cache: HIT` — nothing has been redeployed since 11 Aug. The seven items the rollback re-broke are all still regressed: BUG-01 (Privacy Policy / Terms & Conditions / Share Your Feedback each ×2), BUG-02 (7 `<video>` tags, 0 `poster=`, 0 `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time" present, 0 `list-style` rules), SEO-01 (0 `<meta name="description">`), SEO-04 (`<title>Home - Investing For Mummies</title>` ×2), UX-01 ("Products" heading, 0 "Quick Links"), UX-02 (0 gmail, 0 `tel:`, address modal-only). **Re-deploying the 8 Aug build fixes all seven at once — still the single highest-value action available, now 3 days unactioned.** Open items re-confirmed with fresh evidence: BUG-05 (headless render — TrustedSite badge x 1348–1440 / y 862–900 z 1000003 vs WhatsApp pill x 1190–1430 / y 810–890 z 999999999, **82×28px overlap**, pill on top; screenshot shows the badge clipped to "…nSite®"), SEO-05 (33 of 35 imgs empty `alt=""`), SEO-06 (/resources 200 at 144,873 bytes, 475-char visible body, still "Coming Soon" + old "Products" footer), UX-03 (`#ifmHomeBatchGrid` null, 0 cards; `/api/batches` `"live":[]`, July upcoming entry now 7+ weeks stale), UX-05 (0 "waitlist"/"notify me", 5× plain "Reserve →"), UX-06 (4-slide 1,512px track in a 748px viewport = 2 cards visible, 4 dots), UX-07 (0 "seat" matches despite `"capacity":6` in the API), NICE-03 (0 EMI/instalment matches) all STILL PENDING; NICE-01 STILL IN PROGRESS (same 10 schema types, no LocalBusiness/Course/Event). Two smaller defects unchanged under FIXED items: the cover asset `Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still **404** (16 days), and the footer Games link still has no `target="_blank"`. Verified working: SEO-02 (sitemap.xml, 10 URLs), UX-04 (FAQ on /registernow), NICE-05 (games.investingformummies.com 200).
- 2026-08-15: No status changes — all 9 open items re-verified as still open, and **the 10 Aug rollback is still live (day 5)**. No check ran on 14 Aug, so this entry covers both days. The homepage body is byte-identical for the fifth consecutive day: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (unchanged since 11 Aug), `x-vercel-cache: HIT`; the `last-modified` header now moves with each CDN cache regeneration rather than a build, and a byte-for-byte comparison of two fetches 28 hours apart confirms the content is identical. The seven items the rollback re-broke are all still regressed: BUG-01 (Privacy Policy / Terms & Conditions / Share Your Feedback each ×2), BUG-02 (7 `<video>` tags, 0 `poster=`, 0 `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time" present, 0 `list-style` rules), SEO-01 (0 `<meta name="description">`), SEO-04 (`<title>Home - Investing For Mummies</title>` ×2), UX-01 ("Products" heading, 0 "Quick Links"), UX-02 (0 gmail, 0 `tel:`, address modal-only). **Re-deploying the 8 Aug build fixes all seven at once — still the single highest-value action available, now 5 days unactioned.** ⚠️ One genuine change since the last check: **/registernow was redeployed** (28,093 → 28,872 bytes, `last-modified: Thu, 13 Aug 2026 12:02:28 GMT`) with more batches — **7× "Reserve →"** CTAs (was 5×) and a new **₹12,000** price point. It still adds no waitlist (UX-05), no seat counts (UX-07) and no instalment/EMI copy (NICE-03), and none of it reached the homepage, whose batch grid is still missing (UX-03). Open items re-confirmed with fresh evidence: BUG-05 (headless render — TrustedSite badge x 1348–1440 / y 862–900 z 1000003 vs WhatsApp pill x 1190–1430 / y 810–890 z 999999999, **82×28px overlap**, pill on top; screenshot shows the badge clipped to "…dSite®" with "CERTIFIED SECURE" below — five days without a pixel moving), SEO-05 (33 of 35 imgs empty `alt=""`), SEO-06 (/resources 200 at 144,873 bytes, 475-char visible body, still "Coming Soon" + old "Products" footer), UX-03 (`#ifmHomeBatchGrid` null, 0 cards; `/api/batches` `"live":[]`, July upcoming entry now 7+ weeks stale), UX-05, UX-06 (4-slide 358px-per-card track in a 748px viewport = 2 cards visible, 4 dots), UX-07 (0 "seat" matches despite `"capacity":6` in the API), NICE-03 all STILL PENDING; NICE-01 STILL IN PROGRESS (same 10 schema types, no LocalBusiness/Course/Event). Two smaller defects unchanged under FIXED items: the cover asset `Gemini_Generated_Image_ee4fu7ee4fu7ee4f-1024x572.png` still **404** (18 days), and the footer Games link still has no `target="_blank"`. Verified working: SEO-02 (sitemap.xml, 10 URLs), UX-04 (FAQ on /registernow), NICE-05 (games.investingformummies.com 200). **Second run same day (15 Aug, 13:20 IST): re-verified end-to-end, nothing moved** — homepage still md5 `a7ff9af0d010c975ec945e50c9bb29d4` / 255,614 bytes (`x-vercel-cache: HIT`), BUG-05 overlap re-measured at exactly 82×28px with the pill still on top, /registernow unchanged at 28,872 bytes, missing cover asset still 404. No status changes; no new item notes added — the morning notes still describe live state accurately.
- 2026-08-16: No status changes — all 9 open items re-verified as still open, and **the 10 Aug rollback is still live (day 6)**. The homepage body is byte-identical for the sixth consecutive day: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (unchanged since 11 Aug), `x-vercel-cache: HIT`, `age: 173134`; `last-modified` now reads 14 Aug but that is the CDN cache regenerating, not a build. The seven items the rollback re-broke are all still regressed: BUG-01 ("Privacy Policy"/"Terms & Conditions"/"Share Your Feedback" each ×2), BUG-02 (7 `<video>`, 0 `poster=`, 0 `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time" live, 0 `list-style` rules), SEO-01 (0 meta descriptions), SEO-04 (`<title>` = "Home - Investing For Mummies", ×2), UX-01 (footer heading "Products", 0 "Quick Links"), UX-02 (0 email, 0 `tel:`). **Re-deploying the 8 Aug build fixes all seven at once — one action, unactioned for six days.** Open items re-confirmed: BUG-05 (headless render 1440×900 — TrustedSite badge x 1348–1440/y 862–900 z 1000003 vs WhatsApp pill x 1190–1430/y 810–890 z 999999999, **82×28px overlap**, pixel-identical for six days), SEO-05 (35 imgs, 33 empty `alt=""`), SEO-06 (/resources 144,873 bytes, 475-char visible body, "Coming Soon" + old "Products" footer, double title), UX-03 (`#ifmHomeBatchGrid` null, 0 cards; `/api/batches` still `"live":[]` with a July-2026 placeholder now 7+ weeks stale), UX-05 (0 "waitlist"/"notify me" on /registernow, byte-identical to 13 Aug redeploy, 7× "Reserve →"), UX-06 (4-slide track, 2 cards visible, 4 dots), UX-07 (0 "seat(s)" matches despite `/api/batches` exposing `"capacity":6`), NICE-01 (still no LocalBusiness/Course/Event), NICE-03 (0 EMI/instalment matches). BUG-03 caveat: an asset sweep of all 38 homepage image sources found **4** 404s — all size variants of the one missing `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` cover background, now 19 days unfixed. Verified working: SEO-02 (sitemap.xml 200, 10 URLs), UX-04 (FAQ on /registernow), NICE-05 (games.investingformummies.com 200). Total unchanged: 12 of 18 FIXED, 6 remaining (5 pending core + NICE-01 in progress); NICE-03 pending.
- 2026-08-17: No status changes — **day 7 of the 10 Aug homepage rollback**, and the site is byte-identical to 11–16 Aug (255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"`, `x-vercel-cache: HIT`). The seven items the rollback re-broke are all still live: BUG-01 (legal bar ×2), BUG-02 (7 videos, 0 posters/0 `#t=0.5`), BUG-04 ("mutual o funds" back, 0 `list-style` rules), SEO-01 (0 meta descriptions), SEO-04 (`<title>` = "Home - Investing For Mummies", ×2), UX-01 (footer "Products", 0 "Quick Links"), UX-02 (0 email, 0 `tel:`). Re-deploying the 8 Aug build still fixes all seven in one action. Also unchanged: BUG-05 (82×28px widget overlap, geometry identical for 7 days), BUG-03's 4 × 404 `Gemini_Generated_Image_ee4fu7ee4fu7ee4f*.png` variants (20 days), SEO-05 (33/35 empty `alt`), SEO-06 (/resources still 475 chars of "Coming Soon"), UX-03 (`#ifmHomeBatchGrid` null, 0 cards, `/api/batches` `live:[]` 7+ weeks stale), UX-05 (0 waitlist), UX-06 (4-slide carousel, 2 visible), UX-07 (0 "seats"), NICE-01 (still no LocalBusiness/Course/Event), NICE-03 (0 EMI mentions), NICE-04 (footer Games link still lacks `target="_blank"`). Totals: 12 of 18 FIXED on paper, but 7 of those 12 are actively regressed live — **5 genuinely fixed**, 13 effectively open.
- 2026-08-19: No status changes — **day 9 of the 10 Aug homepage rollback**. No check ran on 18 Aug, so this entry covers both days. The homepage is still byte-identical to 11–17 Aug: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` (unchanged for nine days — the etag, not `last-modified`, is the reliable signal; today's fetch was an `x-vercel-cache: MISS` so `last-modified` merely echoed the fetch time). All seven rollback-induced regressions re-confirmed live: BUG-01 ("Privacy Policy"/"Share Your Feedback"/"Terms & Conditions" each ×2), BUG-02 (7 `<video>`, zero `poster=`, zero `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time", zero `list-style` rules), SEO-01 (zero meta descriptions), SEO-04 (`<title>` "Home - Investing For Mummies" ×2), UX-01 (footer "Products", zero "Quick Links"), UX-02 (zero email, zero `tel:`). **Re-deploying the 8 Aug build still fixes all seven in one action.** All 9 genuinely-open items re-verified as still open: BUG-05 (headless render — TrustedSite badge x 1348–1440/y 862–900 z1000003 vs WhatsApp pill x 1190–1430/y 810–890 z999999999, **82×28px overlap**, screenshot confirms the pill clipping the badge to "…nSite®"), SEO-05 (35 `<img>`, 33 empty `alt=""`), SEO-06 (/resources 200 at 144,873 bytes, 475-char body, still "Coming Soon" + old "Products" footer), UX-03 (`#ifmHomeBatchGrid` null in rendered DOM, 0 cards; `/api/batches` still `"live":[]` with a July 2026 upcoming entry now 7+ weeks stale), UX-05 (zero "waitlist"/"notify me" on /registernow), UX-06 (4-slide 358px track in a 748px viewport = 2 cards visible, 4 dot positions), UX-07 (zero "seat"/"seats" on /registernow despite `"capacity":6` in the API), NICE-01 (single ld+json block, still no LocalBusiness/Course/Event), NICE-03 (zero EMI/instalment/payment-plan matches). BUG-03 asset sweep: 38 unique homepage image sources, **4 non-200s** — all size variants of `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png`, now **22 days** unfixed. /registernow unchanged since 13 Aug (28,872 bytes, `etag: "fe7ae5d3…"`, 7× "Reserve →"). Totals unchanged: 12 of 18 FIXED on paper, but 7 of those are regressed live.
- 2026-08-20: No status changes — nothing newly fixed, nothing improved, nothing regressed further. **Day 10 of the 10 Aug rollback.** The homepage is byte-identical for the tenth consecutive day: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` unchanged since 11 Aug, `x-vercel-cache: HIT` (`age: 80119`) — the `last-modified` header tracks CDN cache regeneration, not a build. All seven items the rollback re-broke remain regressed: BUG-01 (Privacy Policy ×2, Share Your Feedback ×2, Terms & Conditions ×2), BUG-02 (7 `<video>` tags, 0 posters, 0 `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time" live, 0 `list-style` rules), SEO-01 (0 meta descriptions), SEO-04 (`<title>` "Home - Investing For Mummies" ×2), UX-01 (footer "Products", 0 "Quick Links"), UX-02 (0 email, 0 `tel:` links). **Re-deploying the 8 Aug build still fixes all seven in one action** — ten days and counting on a single-action fix. Also unchanged: BUG-03's 4 image 404s (all size variants of `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png`, 23 days), SEO-03 (`og:url`="/" and relative `og:image` still unresolvable for scrapers), BUG-05 (headless render: 82×28px overlap, WhatsApp pill z-index 999999999 over badge 1000003 — tenth day not a pixel moved), SEO-05 (35 imgs, 33 empty `alt=""`), SEO-06 (/resources still 475 chars of "Coming Soon" + old "Products" footer), UX-03 (`#ifmHomeBatchGrid` null, 0 cards, `/api/batches` `"live":[]` with a 7+ week stale "July 2026" upcoming entry), UX-05 (0 "waitlist"/"notify me" on /registernow, 7× "Reserve →"), UX-06 (4-slide 1,512px track in a 748px viewport — 2 cards visible, 4 dots), UX-07 (0 "seat"/"seats" despite `"capacity":6` in the API), NICE-01 (no LocalBusiness/Course/Event schema), NICE-03 (0 EMI/instalment mentions). NICE-04 holds, with the footer Games link still missing `target="_blank"`. Totals unchanged: **12 of 18 core items FIXED on the checklist — but 7 of those 12 are regressed on the live site**, so a visitor effectively sees 5 fixed. 6 remaining (5 PENDING core + NICE-01 IN PROGRESS); NICE-03 also PENDING.
- 2026-08-22: No status changes — nothing newly fixed, nothing improved, nothing regressed further. **Day 12 of the 10 Aug rollback.** No check ran on 21 Aug, so this entry covers both days. The homepage is byte-identical for the twelfth consecutive day: 255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` unchanged since 11 Aug. Today's fetch was deliberately taken on a **cold cache** (`x-vercel-cache: MISS`, `age: 0`) and the body is byte-for-byte identical to the cached copy — so the stall is confirmed **at origin**, not just at the CDN edge; `last-modified` echoing the fetch time is cache regeneration, not a build. All seven items the rollback re-broke remain regressed: BUG-01 (Privacy Policy ×2, Share Your Feedback ×2, Terms & Conditions ×2), BUG-02 (7 `<video>` tags, 0 posters, 0 `#t=0.5`), BUG-04 ("mutual o funds" + "o How money grows over time" live, 0 `list-style` rules), SEO-01 (0 meta descriptions), SEO-04 (`<title>` "Home - Investing For Mummies" ×2), UX-01 (footer "Products" ×2, 0 "Quick Links"), UX-02 (0 email addresses of any kind, 0 `tel:` links). **Re-deploying the 8 Aug build still fixes all seven in one action** — twelve days, a full sprint plus, on a single-action fix. Also unchanged: BUG-03's 4 image 404s (all size variants of `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` out of 38 unique homepage image sources — 25 days), SEO-03 (`og:url`="/" and relative `og:image` still unresolvable for scrapers, `og:title`/`og:description` still the degraded body-copy dump), BUG-05 (headless render at 1440×900: TrustedSite badge x 1348–1440/y 862–900 z1000003 vs WhatsApp pill x 1190–1430/y 810–890 z999999999 = **82×28px overlap**, pill on top — twelfth day not a pixel moved; the screenshot step timed out this run but every measured value matches the eleven previously screenshot-confirmed renders), SEO-05 (35 imgs, 33 empty `alt=""`), SEO-06 (/resources 200 at 144,873 bytes, `etag: "9a94c23f…"`, 475-char visible body of "Coming Soon" + old "Products" footer, double title), UX-03 (`#ifmHomeBatchGrid` null in rendered DOM, 0 cards, `/api/batches` `"live":[]` with a 7+ week stale "July 2026" upcoming entry), UX-05 (0 "waitlist"/"notify me" on /registernow, byte-identical to 15–20 Aug at 28,872 bytes, 7× "Reserve →"), UX-06 (4-slide 1,512px track in a 748px viewport — 2 cards visible, 4 dots), UX-07 (0 "seat"/"seats" despite `"capacity":6` in the API), NICE-01 (single ld+json block, still no LocalBusiness/Course/Event), NICE-03 (0 EMI/instalment/payment-plan mentions on either page). NICE-04 holds, with the footer Games link still missing `target="_blank"`. Verified working: SEO-02 (sitemap.xml 200, valid urlset), UX-04 (FAQ live on /registernow). Totals unchanged: **12 of 18 core items FIXED on the checklist — but 7 of those 12 are regressed on the live site**, so a visitor effectively sees 5 fixed. 6 remaining (5 PENDING core + NICE-01 IN PROGRESS); NICE-03 also PENDING.
- 2026-08-25: No status changes — nothing newly fixed, nothing improved, nothing regressed further. **Day 15 of the 10 Aug rollback.** No checks ran on 23 or 24 Aug, so this entry covers all three days. The homepage is still byte-identical to 11–22 Aug (255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"`, unchanged since 11 Aug; `last-modified` only tracks CDN cache regeneration). The seven items the rollback re-broke (BUG-01, BUG-02, BUG-04, SEO-01, SEO-04, UX-01, UX-02) are all still regressed — one redeploy of the 8 Aug build fixes all seven at once. All 9 genuinely open items re-verified as still open: BUG-05 (82×28px WhatsApp/TrustedSite overlap, screenshot-confirmed), SEO-05 (35 imgs, 33 empty `alt=""`), SEO-06 (/resources still 475 chars of "Coming Soon"), UX-03 (`#ifmHomeBatchGrid` missing, `/api/batches` live:[] and 8 weeks stale), UX-05 (zero "waitlist"), UX-06 (4-slide carousel, 2 cards visible), UX-07 (zero "seats"), NICE-01 (no LocalBusiness/Course/Event schema), NICE-03 (zero EMI/instalment mentions). Also unchanged: the `Gemini_Generated_Image_ee4fu7ee4fu7ee4f.png` 404 (4 size variants, 28 days) and the footer Games link with no `target="_blank"`.
- 2026-08-26: **No status changes — day 16 of the 10 Aug homepage rollback.** The homepage is byte-identical for the sixteenth consecutive day (255,614 bytes, `etag: "a7ff9af0d010c975ec945e50c9bb29d4"` unchanged since 11 Aug), confirmed at origin with a cold-cache fetch. All seven items the rollback re-broke remain regressed: BUG-01 (legal bar ×2), BUG-02 (7 videos, 0 posters), BUG-04 ("mutual o funds" live), SEO-01 (0 meta descriptions), SEO-04 (title "Home - Investing For Mummies" ×2), UX-01 (footer "Products"), UX-02 (0 mailto/tel/email). BUG-03's 4 missing image variants still 404 (29 days). All 9 genuinely-open items re-verified as still open: BUG-05 (82×28px overlap re-confirmed by render + screenshot), SEO-05 (33 of 35 imgs empty alt), SEO-06 (/resources 475-char "Coming Soon"), UX-03 (`#ifmHomeBatchGrid` null, `/api/batches` live:[] — 8+ weeks stale), UX-05 (0 waitlist), UX-06 (4-slide carousel, 2 visible), UX-07 (0 "seats"), NICE-03 (0 EMI/instalment); NICE-01 still IN PROGRESS. Totals: 12 of 18 core items FIXED on paper, but 7 of those are regressed live — effective live state is 5 of 18.
